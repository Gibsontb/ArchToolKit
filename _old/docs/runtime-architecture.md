# Runtime Architecture

The runtime UI lives in:

13_Web/static

Generated runtime assets are stored in:

13_Web/static/generated

The runtime loads catalog data, schemas, and generated artifacts to power the toolkit UI.
