# ATK Data Layout: Raw vs Normalized

- `data/raw/**` : raw exports (CSV/XLSX/PDF/etc). Never edited.
- `data/imports/**` : normalized JSON used by ATK scripts.
- `data/overrides/**` : human edits (CIDRs, mappings, policy).
- `reports/**` : generated output (do not commit).

