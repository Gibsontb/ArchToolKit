/**
 * The PowerShell command catalogue.
 *
 * What is here is not a copy of the help. The syntax line is a correct, complete
 * invocation you can paste, and the note is the thing that is true, not obvious,
 * and costs an afternoon to discover — that `Get-ADUser` returns a fixed set of
 * properties unless you ask for more, that `Get-WmiObject` is gone in
 * PowerShell 7, that `Export-Csv` without `-NoTypeInformation` writes a header
 * line nothing can read.
 *
 * An entry without a note like that is a worse version of `Get-Help`, and is
 * not worth having.
 */

import { group,                   } from './catalog.js';

export const POWERSHELL_CATALOG                          = [
  group('powershell', 'Objects and the pipeline', [
    {
      name: 'Get-Member',
      task: 'see what properties and methods an object actually has',
      syntax: 'Get-Process | Get-Member -MemberType Property',
      effect: 'read',
      note: 'The first thing to run when a property "does not exist". It is also how you find out that what looks like a string is an object with a ToString() that flatters it.',
    },
    {
      name: 'Select-Object',
      task: 'pick properties, take the first or last N, or add a calculated column',
      syntax: "Get-Process | Select-Object Name, Id, @{ n = 'MemoryMB'; e = { [math]::Round($_.WorkingSet / 1MB, 1) } } -First 10",
      parameters: [
        { name: '-First / -Last', what: 'Take N. -First stops the pipeline early, which matters on a slow source.' },
        { name: '-ExpandProperty', what: 'Return the property value itself rather than an object wrapping it.' },
        { name: '-Unique', what: 'Deduplicate — but only on the whole object, so select the property first.' },
      ],
      effect: 'read',
      note: 'A calculated property is @{ n = "Name"; e = { $_ } }. The long forms are Name and Expression; n and e are the aliases everyone uses.',
      related: ['ps.sort-object', 'ps.group-object'],
    },
    {
      name: 'Where-Object',
      task: 'filter a pipeline',
      syntax: "Get-Service | Where-Object { $_.Status -eq 'Running' -and $_.StartType -eq 'Automatic' }",
      effect: 'read',
      note: 'Filter as early as possible, and prefer a command\u2019s own -Filter parameter over Where-Object: Get-ADUser -Filter filters at the directory, Where-Object filters after every object has already been fetched.',
      related: ['ps.select-object'],
    },
    {
      name: 'ForEach-Object',
      task: 'run something for each item in a pipeline',
      syntax: 'Get-Content servers.txt | ForEach-Object -ThrottleLimit 10 -Parallel { Test-Connection $_ -Count 1 -Quiet }',
      parameters: [
        { name: '-Parallel', what: 'PowerShell 7 only. Each iteration is a separate runspace.' },
        { name: '-ThrottleLimit', what: 'How many at once. Five by default, which is usually too few.' },
      ],
      effect: 'changes',
      note: 'Inside -Parallel, variables from outside need $using: to be visible, and $_ is the only thing that crosses automatically. That single rule is most of the trouble people have with it.',
      availability: '-Parallel is PowerShell 7.0 and later',
    },
    {
      name: 'Sort-Object',
      task: 'sort, or deduplicate on a property',
      syntax: 'Get-ChildItem | Sort-Object Length -Descending | Select-Object -First 20',
      effect: 'read',
      note: 'Sort-Object -Unique deduplicates on the sort property, which is usually what people want and is not what Select-Object -Unique does.',
    },
    {
      name: 'Group-Object',
      task: 'count by a property, or bucket objects',
      syntax: 'Get-EventLog -LogName System -Newest 500 | Group-Object Source | Sort-Object Count -Descending',
      effect: 'read',
      note: '-AsHashTable -AsString turns the result into a lookup table, which is far faster than repeatedly filtering a list inside a loop.',
    },
    {
      name: 'Measure-Object',
      task: 'count, sum or average a property',
      syntax: 'Get-ChildItem -Recurse -File | Measure-Object Length -Sum -Average -Maximum',
      effect: 'read',
      note: 'The result is an object, so the number is in .Sum or .Count — not the thing itself. `(… | Measure-Object).Count` is the idiom, and it is why so many scripts have a stray `.Count` in them.',
    },
    {
      name: 'Compare-Object',
      task: 'diff two lists or two sets of objects',
      syntax: 'Compare-Object (Get-Content before.txt) (Get-Content after.txt) -IncludeEqual',
      effect: 'read',
      note: 'SideIndicator is <= for the reference side and => for the difference side. -PassThru returns the objects instead of the wrapper, which is what you want when feeding the result onward.',
    },
    {
      name: 'ConvertTo-Json',
      task: 'turn objects into JSON',
      syntax: 'Get-Service | Select-Object Name, Status | ConvertTo-Json -Depth 5',
      effect: 'read',
      note: 'The default depth is 2, and anything deeper is silently replaced with a type name. That truncation is the single most common surprise in PowerShell JSON — always set -Depth.',
      related: ['ps.convertfrom-json'],
    },
    {
      name: 'ConvertFrom-Json',
      task: 'parse JSON into objects',
      syntax: 'Get-Content config.json -Raw | ConvertFrom-Json',
      effect: 'read',
      note: 'Use -Raw on Get-Content, or the file arrives as an array of lines and the parse fails on the first one. -AsHashtable gives a hashtable instead of PSCustomObject, which is what you want for a case-sensitive lookup.',
    },
    {
      name: 'Export-Csv',
      task: 'write objects to a CSV',
      syntax: 'Get-Process | Export-Csv -Path processes.csv -NoTypeInformation -Encoding utf8',
      effect: 'changes',
      note: 'Without -NoTypeInformation the first line is a #TYPE comment that breaks most readers. It is the default in PowerShell 6 and later and not in 5.1, so write it explicitly either way.',
      related: ['ps.import-csv'],
    },
    {
      name: 'Import-Csv',
      task: 'read a CSV as objects',
      syntax: 'Import-Csv users.csv -Delimiter "," | Where-Object Department -eq "Finance"',
      effect: 'read',
      note: 'Every column comes back as a string, including the numeric ones — so a sort on a "Size" column sorts 100 before 9. Cast it: [int]$_.Size.',
    },
    {
      name: 'Tee-Object',
      task: 'send a pipeline to a file and onward at the same time',
      syntax: 'Get-ChildItem -Recurse | Tee-Object -FilePath inventory.txt | Measure-Object',
      effect: 'changes',
      note: '-Variable captures to a variable instead of a file, which is how you keep a copy of a pipeline without running it twice.',
    },
    {
      name: 'Out-GridView',
      task: 'open results in a sortable, filterable window',
      syntax: 'Get-Process | Out-GridView -PassThru | Stop-Process -WhatIf',
      effect: 'read',
      note: '-PassThru turns it into a picker: whatever you select comes back down the pipeline. It is the quickest interactive filter PowerShell has, and it is Windows only.',
      availability: 'Windows only',
    },
    {
      name: 'Write-Output',
      task: 'emit an object from a function',
      syntax: 'Write-Output $result',
      effect: 'read',
      note: 'Write-Host writes to the screen and cannot be captured, redirected or piped. In a function that is meant to return something, it is almost always the wrong choice — use Write-Output, or just leave the object on a line by itself.',
      related: ['ps.write-verbose'],
    },
    {
      name: 'Write-Verbose',
      task: 'log progress that only shows with -Verbose',
      syntax: 'Write-Verbose "Processing $name" -Verbose:$VerbosePreference',
      effect: 'read',
      note: 'It only appears when the caller asks, which is exactly what you want for progress. In an advanced function, -Verbose on the call sets it automatically.',
    },
    {
      name: 'Write-Progress',
      task: 'show a progress bar for a long loop',
      syntax: 'Write-Progress -Activity "Scanning" -Status "$i of $total" -PercentComplete ($i / $total * 100)',
      effect: 'read',
      note: 'Calling it on every iteration of a fast loop is slower than the work itself. Update it every few hundred items, and call it with -Completed at the end or the bar stays on screen.',
    },
    {
      name: 'Start-Sleep',
      task: 'pause between retries or polls',
      syntax: 'Start-Sleep -Seconds 5',
      effect: 'read',
      note: '-Milliseconds exists for short waits. A retry loop should back off — doubling the wait each time — rather than sleeping the same amount forever.',
    },
    {
      name: 'Measure-Command',
      task: 'time how long something takes',
      syntax: 'Measure-Command { Get-ChildItem C:\\ -Recurse -File }',
      effect: 'read',
      note: 'The output of the scriptblock is discarded, not returned. To time something and keep the result, assign inside the block to a variable declared outside it.',
    },
    {
      name: 'Set-StrictMode',
      task: 'turn typos into errors instead of empty strings',
      syntax: 'Set-StrictMode -Version Latest',
      effect: 'read',
      note: 'Without it, a misspelled variable is silently an empty string and the script carries on doing the wrong thing. Put it at the top of everything.',
    },
  ]),

  group('powershell', 'Files and paths', [
    {
      name: 'Get-ChildItem',
      task: 'list files and folders',
      syntax: 'Get-ChildItem -Path C:\\Logs -Recurse -File -Filter *.log -ErrorAction SilentlyContinue',
      parameters: [
        { name: '-Filter', what: 'Filtered by the provider, so far faster than -Include on a large tree.' },
        { name: '-Force', what: 'Include hidden and system items.' },
        { name: '-Depth', what: 'Bound the recursion. -Recurse on C:\\ without it is a long afternoon.' },
      ],
      effect: 'read',
      note: '-Filter is handled by the filesystem; -Include is handled by PowerShell after the items are enumerated. On a large tree the difference is minutes.',
    },
    {
      name: 'Get-Content',
      task: 'read a file, or follow one as it is written',
      syntax: 'Get-Content -Path app.log -Tail 50 -Wait',
      parameters: [
        { name: '-Raw', what: 'One string rather than an array of lines. Needed for anything that parses the whole file.' },
        { name: '-Tail', what: 'The last N lines, read from the end rather than the start.' },
        { name: '-Wait', what: 'Follow the file as it grows, like tail -f.' },
      ],
      effect: 'read',
      note: 'Without -Raw you get an array, and without -Tail on a large file you read all of it into memory first. Both of those turn a quick look into a wait.',
    },
    {
      name: 'Set-Content',
      task: 'write a file, replacing what is there',
      syntax: 'Set-Content -Path config.txt -Value $lines -Encoding utf8',
      effect: 'changes',
      note: 'Windows PowerShell 5.1 writes UTF-16 by default and PowerShell 7 writes UTF-8 with no BOM. Set -Encoding explicitly or the same script produces different files on different machines.',
      related: ['ps.add-content', 'ps.out-file'],
    },
    {
      name: 'Add-Content',
      task: 'append to a file',
      syntax: 'Add-Content -Path audit.log -Value "$(Get-Date -f s) $message"',
      effect: 'changes',
      note: 'It opens and closes the file on every call, so appending in a tight loop is slow. For anything hot, use a StreamWriter or collect and write once.',
    },
    {
      name: 'Out-File',
      task: 'send pipeline output to a file',
      syntax: 'Get-Process | Out-File -FilePath processes.txt -Width 200',
      effect: 'changes',
      note: 'It writes the formatted display, not the objects — so columns get truncated at the console width unless you set -Width. Export-Csv or ConvertTo-Json keeps the data.',
    },
    {
      name: 'Test-Path',
      task: 'check whether a path exists',
      syntax: 'if (-not (Test-Path -Path $folder -PathType Container)) { New-Item -Path $folder -ItemType Directory }',
      effect: 'read',
      note: '-PathType Container or Leaf distinguishes a folder from a file. Without it, a file called "logs" satisfies a test meant for a folder.',
    },
    {
      name: 'New-Item',
      task: 'create a file, folder or registry key',
      syntax: 'New-Item -Path C:\\Logs -ItemType Directory -Force',
      effect: 'changes',
      note: '-Force on a directory creates missing parents and does not complain if it already exists, which makes it idempotent. -Force on a file truncates it, which does not.',
    },
    {
      name: 'Copy-Item',
      task: 'copy files or folders',
      syntax: 'Copy-Item -Path .\\src\\* -Destination \\\\server\\share -Recurse -Force',
      effect: 'changes',
      note: 'For anything large or over a network, robocopy is faster, restartable and reports properly. Copy-Item is for a handful of files.',
      related: ['cmd.robocopy'],
    },
    {
      name: 'Move-Item',
      task: 'move or rename',
      syntax: 'Move-Item -Path old.log -Destination archive\\old-$(Get-Date -f yyyyMMdd).log',
      effect: 'changes',
      note: 'Across volumes it is a copy then a delete, so it is not atomic and an interruption can leave both. On the same volume it is a rename and is atomic.',
    },
    {
      name: 'Remove-Item',
      task: 'delete files or folders',
      syntax: 'Remove-Item -Path C:\\Temp\\* -Recurse -Force -WhatIf',
      effect: 'destructive',
      note: '-Recurse -Force on a path built from a variable is the classic way to delete far more than intended. Run it with -WhatIf first, every time, and never let the path come from an argument without validating it.',
    },
    {
      name: 'Rename-Item',
      task: 'rename in place',
      syntax: "Get-ChildItem *.txt | Rename-Item -NewName { $_.Name -replace '\\.txt$', '.log' }",
      effect: 'changes',
      note: '-NewName takes a scriptblock when piped, which is how bulk renames are done. The new name is a name, not a path — moving is Move-Item.',
    },
    {
      name: 'Resolve-Path',
      task: 'turn a relative or wildcard path into a full one',
      syntax: 'Resolve-Path -Path .\\config\\*.json',
      effect: 'read',
      note: 'It throws if the path does not exist. For a path that might not, use -ErrorAction SilentlyContinue, or [System.IO.Path]::GetFullPath() which does not care.',
    },
    {
      name: 'Join-Path',
      task: 'build a path safely',
      syntax: 'Join-Path -Path $root -ChildPath "logs\\$name.log"',
      effect: 'read',
      note: 'It gets the separators right without you thinking about it, including when $root already ends in one. String concatenation is how you end up with C:\\logs\\\\file.',
    },
    {
      name: 'Split-Path',
      task: 'take the folder, the filename or the extension off a path',
      syntax: 'Split-Path -Path $file -Parent',
      effect: 'read',
      note: '-Leaf gives the filename, -Parent the folder, -LeafBase the name without the extension (PowerShell 6+), -Qualifier the drive.',
    },
    {
      name: 'Get-FileHash',
      task: 'checksum a file',
      syntax: 'Get-FileHash -Path installer.msi -Algorithm SHA256',
      effect: 'read',
      note: 'The default is SHA256, which is the right default. Comparing hashes is how you tell whether two files are the same when the sizes match but you do not trust them.',
    },
    {
      name: 'Compress-Archive',
      task: 'make a zip archive of files or folders',
      syntax: 'Compress-Archive -Path .\\logs\\* -DestinationPath logs.zip -CompressionLevel Optimal',
      effect: 'changes',
      note: 'It is slow and it has a 2GB limit in older versions. For anything large, use tar.exe, which has shipped with Windows since 1803.',
    },
    {
      name: 'Get-Acl',
      task: 'read NTFS permissions',
      syntax: 'Get-Acl -Path \\\\server\\share\\Finance | Select-Object -ExpandProperty Access',
      effect: 'read',
      note: 'AreAccessRulesProtected is the one that matters in an audit: it means inheritance is disabled, so the folder keeps whatever it was given and the parent no longer governs it.',
      related: ['ps.set-acl'],
    },
    {
      name: 'Set-Acl',
      task: 'apply NTFS permissions',
      syntax: '$acl = Get-Acl $path; $acl.AddAccessRule($rule); Set-Acl -Path $path -AclObject $acl',
      effect: 'changes',
      note: 'You read, modify and write back — there is no "add one rule" command. Setting an ACL you built from scratch replaces everything, including the inherited rules you did not mean to remove.',
    },
  ]),

  group('powershell', 'Text and data', [
    {
      name: 'Select-String',
      task: 'search files for a pattern — the PowerShell grep',
      syntax: 'Select-String -Path *.log -Pattern "ERROR|FATAL" -Context 2, 2',
      parameters: [
        { name: '-Context', what: 'Lines before and after each match.' },
        { name: '-SimpleMatch', what: 'Literal, not a regex. Much faster and no escaping.' },
        { name: '-List', what: 'Stop at the first match per file — which file, not which line.' },
      ],
      effect: 'read',
      note: 'It returns MatchInfo objects, not strings: .Line is the text, .LineNumber and .Filename are the useful parts. Piping it to something expecting strings quietly gets the ToString().',
    },
    {
      name: '-replace',
      task: 'regex replace in a string',
      syntax: "$text -replace '(?<year>\\d{4})-(\\d{2})', '${year}'",
      effect: 'read',
      note: 'It is a regex operator, so . and ( are special. For a literal replacement use .Replace(), which is also faster. Named groups in the replacement are ${name}, with the braces.',
    },
    {
      name: '-match',
      task: 'test a string against a regex and capture groups',
      syntax: "if ($line -match '^(?<level>\\w+)\\s+(?<message>.*)$') { $Matches.level }",
      effect: 'read',
      note: '$Matches is populated as a side effect and survives until the next -match. Inside a loop that means a failed match leaves the previous one\u2019s values, which is a real source of wrong answers.',
    },
    {
      name: '-split',
      task: 'split a string',
      syntax: "$line -split '\\s+', 3",
      effect: 'read',
      note: 'It splits on a regex by default. The second argument caps the number of pieces, so the last one keeps the rest — which is how you split a log line into level, timestamp and the whole message.',
    },
    {
      name: 'ConvertFrom-StringData',
      task: 'parse key=value text into a hashtable',
      syntax: 'Get-Content settings.ini -Raw | ConvertFrom-StringData',
      effect: 'read',
      note: 'Backslashes are treated as escapes, so Windows paths in the file need doubling. That surprise is why most people write their own parser instead.',
    },
    {
      name: 'Format-Table',
      task: 'lay results out in columns',
      syntax: 'Get-Process | Format-Table Name, Id, WS -AutoSize',
      effect: 'read',
      note: 'Anything after a Format-* command is no longer an object — it is formatting instructions. Put it last, never in the middle of a pipeline, and never before Export-Csv.',
    },
    {
      name: 'Format-List',
      task: 'show every property of an object, one per line',
      syntax: 'Get-Service wuauserv | Format-List *',
      effect: 'read',
      note: 'Format-List * is the quickest way to see everything an object carries, including the properties the default table view hides.',
    },
    {
      name: 'Out-String',
      task: 'turn formatted output back into a string',
      syntax: 'Get-Process | Format-Table | Out-String -Width 200',
      effect: 'read',
      note: 'Needed when you want the rendered table as text — for an email body, say. -Width stops it wrapping at the console width, which is 80 in a scheduled task.',
    },
    {
      name: '[datetime]::ParseExact',
      task: 'parse a date in a known format',
      syntax: "[datetime]::ParseExact('20260922-143000', 'yyyyMMdd-HHmmss', $null)",
      effect: 'read',
      note: 'Get-Date -Date on an ambiguous string uses the machine\u2019s locale, so 03/04 is March on one machine and April on another. ParseExact removes the ambiguity, which matters for anything parsing logs.',
    },
    {
      name: 'Get-Date',
      task: 'the current time, or format one',
      syntax: "Get-Date -Format 'yyyy-MM-dd HH:mm:ss'",
      effect: 'read',
      note: "-Format s gives a sortable ISO-like string with no spaces, which is what filenames want. -UFormat uses the Unix format specifiers instead, which is occasionally what a tool expects.",
    },
  ]),

  group('powershell', 'Processes and services', [
    {
      name: 'Get-Process',
      task: 'list running processes',
      syntax: 'Get-Process | Sort-Object WS -Descending | Select-Object -First 10 Name, Id, @{ n = "MemoryMB"; e = { [math]::Round($_.WS / 1MB) } }',
      effect: 'read',
      note: 'CPU is total seconds since start, not a percentage — a long-running process always looks busy. For a percentage, sample the counter twice or use Get-Counter.',
    },
    {
      name: 'Stop-Process',
      task: 'kill a process',
      syntax: 'Get-Process notepad | Stop-Process -Force -WhatIf',
      effect: 'destructive',
      note: 'It is a kill, not a close: nothing is saved and no shutdown handler runs. For an application with unsaved state, CloseMainWindow() first and wait.',
    },
    {
      name: 'Start-Process',
      task: 'launch something, optionally waiting for it',
      syntax: 'Start-Process -FilePath msiexec -ArgumentList "/i app.msi /qn" -Wait -PassThru -NoNewWindow',
      parameters: [
        { name: '-Wait', what: 'Block until it exits. Without this the script races ahead.' },
        { name: '-PassThru', what: 'Return the process object, so you can read .ExitCode.' },
        { name: '-RedirectStandardOutput', what: 'Capture output to a file — it cannot go to the pipeline.' },
      ],
      effect: 'changes',
      note: 'Without both -Wait and -PassThru you cannot get the exit code, which means you cannot tell whether it worked. That pair together is the whole point of using it over &.',
    },
    {
      name: 'Get-Service',
      task: 'list or check services',
      syntax: "Get-Service -Name 'W3SVC', 'MSSQLSERVER' | Where-Object Status -ne 'Running'",
      effect: 'read',
      note: 'It does not tell you the service account or the binary path. Get-CimInstance Win32_Service does, which is why any real service audit uses that instead.',
      related: ['ps.get-ciminstance'],
    },
    {
      name: 'Start-Service',
      task: 'start a service and wait for it',
      syntax: "Start-Service -Name W3SVC -PassThru | Where-Object Status -ne 'Running'",
      effect: 'changes',
      note: 'It returns as soon as the service reports started, which is not the same as ready. WaitForStatus() with a timeout is how you actually wait.',
    },
    {
      name: 'Restart-Service',
      task: 'stop and start a service',
      syntax: 'Restart-Service -Name W3SVC -Force',
      effect: 'changes',
      note: '-Force also restarts the dependent services, and without it the stop fails when anything depends on it. Know what depends on it before using -Force in production.',
    },
    {
      name: 'Set-Service',
      task: 'change a service start type or account',
      syntax: 'Set-Service -Name Spooler -StartupType Disabled',
      effect: 'changes',
      note: 'Changing the account here does not grant "log on as a service", so the service will fail to start afterwards until that right is granted separately.',
    },
    {
      name: 'Get-CimInstance',
      task: 'query WMI — the modern way',
      syntax: 'Get-CimInstance -ClassName Win32_LogicalDisk -Filter "DriveType = 3" -ComputerName SRV01',
      effect: 'read',
      note: 'This replaced Get-WmiObject, which does not exist in PowerShell 7 at all. CIM uses WSMan rather than DCOM, so it works through a firewall that allows remoting.',
      related: ['ps.get-wmiobject'],
    },
    {
      name: 'Get-WmiObject',
      task: 'query WMI — the old way',
      syntax: 'Get-WmiObject -Class Win32_OperatingSystem',
      effect: 'read',
      deprecated: 'Get-CimInstance',
      note: 'Removed in PowerShell 6 and later. A script using it works on Windows PowerShell 5.1 and fails outright on 7, which is how scripts break when a machine is upgraded.',
    },
    {
      name: 'Get-Counter',
      task: 'read performance counters',
      syntax: "Get-Counter -Counter '\\Processor(_Total)\\% Processor Time' -SampleInterval 2 -MaxSamples 5",
      effect: 'read',
      note: 'Counter paths are localised — on a German machine the path above does not exist. Use the numeric counter ids, or look them up from the registry, for anything that has to run everywhere.',
    },
    {
      name: 'Get-EventLog',
      task: 'read a classic event log',
      syntax: 'Get-EventLog -LogName System -EntryType Error -Newest 100',
      effect: 'read',
      deprecated: 'Get-WinEvent',
      note: 'It only reads the classic logs, not the newer per-application ones, and it is gone from PowerShell 7. Get-WinEvent reads everything and is far faster with a filter.',
      related: ['ps.get-winevent'],
    },
    {
      name: 'Get-WinEvent',
      task: 'read any event log, fast',
      syntax: "Get-WinEvent -FilterHashtable @{ LogName = 'System'; Level = 1, 2; StartTime = (Get-Date).AddHours(-24) } -MaxEvents 200",
      effect: 'read',
      note: '-FilterHashtable filters at the log; -FilterXPath is more flexible; piping to Where-Object filters after reading every event and is hundreds of times slower. It also throws rather than returning nothing when there are no matches, so wrap it.',
    },
    {
      name: 'New-EventLog',
      task: 'register an event source so a script can write to the log',
      syntax: "New-EventLog -LogName Application -Source 'Automation'",
      effect: 'changes',
      note: 'Needs administrator once, then Write-EventLog works for anyone. A script writing to an unregistered source fails with a permissions error that does not mention registration.',
    },
  ]),

  group('powershell', 'Remoting and sessions', [
    {
      name: 'Invoke-Command',
      task: 'run something on another machine',
      syntax: 'Invoke-Command -ComputerName SRV01, SRV02 -ScriptBlock { Get-Service W3SVC } -ErrorAction Continue',
      parameters: [
        { name: '-ThrottleLimit', what: 'How many machines at once. 32 by default.' },
        { name: '-AsJob', what: 'Return immediately with a job, so you can time it out.' },
        { name: '-ArgumentList', what: 'Values for the scriptblock — local variables are not visible without $using:.' },
      ],
      effect: 'changes',
      note: 'Variables from the calling scope need $using:name inside the scriptblock. Objects come back deserialized, so their methods are gone — call methods on the remote side, not on the result.',
    },
    {
      name: 'New-PSSession',
      task: 'open a reusable session to a machine',
      syntax: 'Get-Content servers.txt | New-PSSession -ThrottleLimit 20',
      effect: 'changes',
      note: 'A session reused across several Invoke-Command calls is far cheaper than reconnecting each time, and it keeps state between them. Close them with Remove-PSSession — an abandoned session holds resources on the far end until it times out.',
    },
    {
      name: 'Enter-PSSession',
      task: 'get an interactive prompt on another machine',
      syntax: 'Enter-PSSession -ComputerName SRV01',
      effect: 'changes',
      note: 'Interactive only — it does nothing useful in a script. Exit-PSSession, or just `exit`, comes back.',
    },
    {
      name: 'Enable-PSRemoting',
      task: 'turn on remoting on a machine',
      syntax: 'Enable-PSRemoting -Force -SkipNetworkProfileCheck',
      effect: 'changes',
      note: 'It starts WinRM, sets it to automatic and adds a firewall rule. -SkipNetworkProfileCheck is needed when a network adapter is marked Public, which a virtual or VPN adapter often is.',
    },
    {
      name: 'Test-WSMan',
      task: 'check whether remoting works to a machine',
      syntax: 'Test-WSMan -ComputerName SRV01',
      effect: 'read',
      note: 'Faster and more specific than trying Invoke-Command and waiting for it to fail. It tells you WinRM answered, not that your account can use it.',
    },
    {
      name: 'Set-Item WSMan:\\localhost\\Client\\TrustedHosts',
      task: 'allow remoting to a machine outside the domain',
      syntax: "Set-Item WSMan:\\localhost\\Client\\TrustedHosts -Value 'SRV01,SRV02' -Force",
      effect: 'changes',
      note: 'It replaces the list rather than adding to it — use -Concatenate to add. TrustedHosts disables server identity checking for those names, so it is a workgroup tool, not a domain one.',
    },
    {
      name: 'Get-Credential',
      task: 'prompt for a username and password',
      syntax: "$credential = Get-Credential -Message 'Account for the target machines'",
      effect: 'read',
      note: 'The password is a SecureString and stays out of the transcript and the process list. Never build a credential from a plain-text password in a script — that is the thing this exists to avoid.',
      related: ['ps.get-secret'],
    },
    {
      name: 'Get-Secret',
      task: 'read a credential from a vault',
      syntax: 'Get-Secret -Name EstateAdmin -Vault CorporateVault',
      module: 'Microsoft.PowerShell.SecretManagement',
      effect: 'read',
      note: 'This is what makes an unattended script possible without a password in it. The vault is per user and per machine, so a scheduled task needs the secret registered under the account it runs as.',
    },
  ]),

  group('powershell', 'Active Directory', [
    {
      name: 'Get-ADUser',
      task: 'find users in Active Directory, with the properties you need',
      syntax: "Get-ADUser -Filter { Enabled -eq $true } -SearchBase 'OU=Staff,DC=example,DC=com' -Properties LastLogonDate, Manager, Department",
      parameters: [
        { name: '-Filter', what: 'Filters at the directory. Far faster than Where-Object.' },
        { name: '-Properties', what: 'Anything beyond the default ten needs naming here.' },
        { name: '-SearchBase', what: 'Limit to an OU rather than the whole domain.' },
      ],
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'Only about ten properties come back by default — LastLogonDate, Manager and Department are not among them, and asking for one you did not request gives you nothing rather than an error. -Properties * is the debugging answer and far too slow for production.',
      related: ['ps.set-aduser', 'ps.get-adcomputer'],
    },
    {
      name: 'Set-ADUser',
      task: 'change a user account',
      syntax: 'Set-ADUser -Identity jbloggs -Department Finance -Manager smanager -Replace @{ extensionAttribute1 = "contractor" }',
      module: 'ActiveDirectory',
      effect: 'changes',
      note: '-Replace, -Add and -Clear are for attributes that have no named parameter. -Replace on a multi-valued attribute replaces all of it, which is rarely what was meant — -Add adds one.',
    },
    {
      name: 'New-ADUser',
      task: 'create a user account',
      syntax: 'New-ADUser -Name "Jane Bloggs" -SamAccountName jbloggs -UserPrincipalName jbloggs@example.com -Path "OU=Staff,DC=example,DC=com" -AccountPassword $secure -Enabled $true',
      module: 'ActiveDirectory',
      effect: 'changes',
      note: 'Without -Enabled $true the account is created disabled, which is usually what you want. Without -AccountPassword it is created with no password and cannot be enabled at all.',
    },
    {
      name: 'Get-ADGroupMember',
      task: 'list who is in a group',
      syntax: "Get-ADGroupMember -Identity 'Domain Admins' -Recursive",
      module: 'ActiveDirectory',
      effect: 'read',
      note: '-Recursive expands nested groups, which is where the interesting members always are. It also fails on a group with more than 5000 members — for those, read the member attribute in pages instead.',
    },
    {
      name: 'Add-ADGroupMember',
      task: 'add someone to a group',
      syntax: "Add-ADGroupMember -Identity 'VPN Users' -Members jbloggs, ssmith",
      module: 'ActiveDirectory',
      effect: 'changes',
      note: 'Adding a member that is already there throws rather than being a no-op, so a re-runnable script has to check first or catch it.',
    },
    {
      name: 'Get-ADComputer',
      task: 'find computer accounts',
      syntax: 'Get-ADComputer -Filter { OperatingSystem -like "*Server*" } -Properties LastLogonDate, OperatingSystemVersion',
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'LastLogonDate comes from lastLogonTimestamp, which replicates lazily and can be up to 14 days behind. A machine that looks 90 days stale may be 76.',
    },
    {
      name: 'Search-ADAccount',
      task: 'find locked, disabled, expired or inactive accounts',
      syntax: 'Search-ADAccount -AccountInactive -TimeSpan 90.00:00:00 -UsersOnly',
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'Far simpler than building the filter by hand, and it gets the lastLogonTimestamp arithmetic right — which is the part people usually get wrong.',
    },
    {
      name: 'Unlock-ADAccount',
      task: 'unlock a locked-out account',
      syntax: 'Unlock-ADAccount -Identity jbloggs',
      module: 'ActiveDirectory',
      effect: 'changes',
      note: 'Unlocking does not reset the password or tell you why it locked. Search the 4740 events on the PDC emulator to find which machine is still presenting the old password.',
    },
    {
      name: 'Get-ADDomainController',
      task: 'list domain controllers, or find the one holding a role',
      syntax: 'Get-ADDomainController -Filter * | Select-Object Name, Site, OperationMasterRoles',
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'Account lockout and password events are written on the PDC emulator, so that is the one to search for them. -Discover -Service PrimaryDC finds it.',
    },
    {
      name: 'Get-ADReplicationFailure',
      task: 'find replication problems',
      syntax: 'Get-ADDomainController -Filter * | ForEach-Object { Get-ADReplicationFailure -Target $_.Name }',
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'Replication failing quietly is how a directory ends up with different answers depending on which controller you asked. Run it across every controller, not just one.',
    },
    {
      name: 'Get-ADObject',
      task: 'query anything in the directory, including deleted objects',
      syntax: "Get-ADObject -Filter 'isDeleted -eq $true' -IncludeDeletedObjects -Properties whenChanged",
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'This is how you find and restore a deleted object, if the recycle bin is enabled. If it is not, the object is a tombstone with most attributes already stripped.',
    },
    {
      name: 'Restore-ADObject',
      task: 'undelete an object',
      syntax: "Get-ADObject -Filter 'Name -like \"*Bloggs*\"' -IncludeDeletedObjects | Restore-ADObject",
      module: 'ActiveDirectory',
      effect: 'changes',
      note: 'Only works when the Active Directory Recycle Bin is enabled, and only within the deleted object lifetime. Enabling the recycle bin is irreversible and should have been done years ago.',
    },
    {
      name: 'Get-ADGroup',
      task: 'find groups in Active Directory and what is in them',
      syntax: 'Get-ADGroup -Filter { GroupCategory -eq "Security" -and GroupScope -eq "Global" } -Properties Description, ManagedBy',
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'ManagedBy is the field that answers "who owns this group", and it is empty on almost every group in almost every directory. Filling it in is the cheapest access-review improvement there is.',
    },
    {
      name: 'Get-ADOrganizationalUnit',
      task: 'list organisational units in Active Directory',
      syntax: 'Get-ADOrganizationalUnit -Filter * -Properties ProtectedFromAccidentalDeletion',
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'ProtectedFromAccidentalDeletion is on by default for new OUs and blocks a delete until it is cleared — which is also why a scripted cleanup fails on it with an unhelpful access denied.',
    },
    {
      name: 'Get-ADDefaultDomainPasswordPolicy',
      task: 'read the domain password policy',
      syntax: 'Get-ADDefaultDomainPasswordPolicy',
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'This is the domain-wide one. A fine-grained policy applied to a group overrides it, and Get-ADUserResultantPasswordPolicy is what tells you which one a given user actually gets.',
    },
    {
      name: 'Get-ADUserResultantPasswordPolicy',
      task: 'find which password policy applies to a user',
      syntax: 'Get-ADUserResultantPasswordPolicy -Identity jbloggs',
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'Returns nothing when no fine-grained policy applies, which means the default policy does — that empty result is an answer, not a failure.',
    },
    {
      name: 'Get-ADFineGrainedPasswordPolicy',
      task: 'list fine-grained password policies',
      syntax: 'Get-ADFineGrainedPasswordPolicy -Filter * | Select-Object Name, Precedence, AppliesTo',
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'Precedence decides which one wins when several apply; lower is stronger. Two policies with the same precedence resolve by GUID, which is effectively at random.',
    },
    {
      name: 'Get-GPO',
      task: 'list Group Policy objects',
      syntax: 'Get-GPO -All | Select-Object DisplayName, GpoStatus, ModificationTime',
      module: 'GroupPolicy',
      effect: 'read',
      note: 'GpoStatus tells you whether half of it is disabled. A GPO with "UserSettingsDisabled" and user settings in it is a common reason a policy "does not apply".',
    },
    {
      name: 'Get-GPOReport',
      task: 'export what a GPO actually contains',
      syntax: 'Get-GPOReport -All -ReportType Html -Path .\\all-gpos.html',
      module: 'GroupPolicy',
      effect: 'read',
      note: 'The XML report is the only practical way to search every GPO for a setting. There is no "find which GPO sets this" command, so people grep the XML.',
    },
    {
      name: 'Backup-GPO',
      task: 'back up Group Policy objects',
      syntax: 'Backup-GPO -All -Path D:\\GPOBackups\\$(Get-Date -f yyyyMMdd) -Comment "Before the change"',
      module: 'GroupPolicy',
      effect: 'changes',
      note: 'A GPO backup does not include the WMI filters or the links. Restoring one puts the settings back and leaves it linked nowhere, which surprises people mid-incident.',
    },
    {
      name: 'Invoke-GPUpdate',
      task: 'refresh policy on a machine',
      syntax: 'Invoke-GPUpdate -Computer SRV01 -Force -RandomDelayInMinutes 0',
      module: 'GroupPolicy',
      effect: 'changes',
      note: 'It schedules a task on the target rather than doing it immediately, and the default random delay is up to ten minutes. -RandomDelayInMinutes 0 is what makes it prompt.',
    },
    {
      name: 'Get-ADTrust',
      task: 'list domain and forest trusts',
      syntax: 'Get-ADTrust -Filter * | Select-Object Name, Direction, TrustType, SelectiveAuthentication',
      module: 'ActiveDirectory',
      effect: 'read',
      note: 'SelectiveAuthentication off on an external trust means everyone in the other forest is Authenticated Users here, which is almost never what anyone intended.',
    },
  ]),

  group('powershell', 'Windows system', [
    {
      name: 'Get-ComputerInfo',
      task: 'everything about a machine in one call',
      syntax: 'Get-ComputerInfo | Select-Object CsName, OsName, OsVersion, CsManufacturer, CsModel, OsLastBootUpTime',
      effect: 'read',
      note: 'One call rather than six WMI queries, but it is slow — several seconds — so it is wrong inside a loop over an estate. For that, query the specific CIM classes.',
    },
    {
      name: 'Get-HotFix',
      task: 'list installed patches',
      syntax: 'Get-HotFix | Sort-Object InstalledOn -Descending | Select-Object -First 10',
      effect: 'read',
      note: 'It lists only what was installed as a hotfix package, so cumulative updates show as one entry and feature updates as none. It is a poor patch inventory on modern Windows.',
    },
    {
      name: 'Get-WindowsFeature',
      task: 'list roles and features on a server',
      syntax: 'Get-WindowsFeature | Where-Object Installed | Select-Object Name, DisplayName',
      effect: 'read',
      availability: 'Windows Server only',
      note: 'On a client, the equivalent is Get-WindowsOptionalFeature -Online, which uses different names for the same things.',
    },
    {
      name: 'Install-WindowsFeature',
      task: 'install a role or feature',
      syntax: 'Install-WindowsFeature -Name Web-Server -IncludeManagementTools -Restart',
      effect: 'changes',
      availability: 'Windows Server only',
      note: 'Without -IncludeManagementTools you get the role and none of the tools to manage it, which is the most common complaint about it. -Restart reboots without asking.',
    },
    {
      name: 'Get-ScheduledTask',
      task: 'list scheduled tasks',
      syntax: "Get-ScheduledTask | Where-Object State -ne 'Disabled' | Get-ScheduledTaskInfo | Sort-Object LastRunTime",
      effect: 'read',
      note: 'Get-ScheduledTask gives the definition and Get-ScheduledTaskInfo gives the history — last run, last result, next run. You need both, and only the second tells you whether it is actually working.',
    },
    {
      name: 'Register-ScheduledTask',
      task: 'create a scheduled task',
      syntax: 'Register-ScheduledTask -TaskName Nightly -Action $action -Trigger $trigger -Principal $principal -Settings $settings -Force',
      effect: 'changes',
      note: 'Built from four separate objects — action, trigger, principal, settings. -Force replaces an existing task of the same name rather than failing, which is what makes it re-runnable.',
    },
    {
      name: 'Get-LocalGroupMember',
      task: 'list local group membership',
      syntax: 'Get-LocalGroupMember -SID S-1-5-32-544',
      effect: 'read',
      note: 'Use the SID, not the name: the Administrators group is called something else on a non-English Windows, and S-1-5-32-544 is the same everywhere.',
    },
    {
      name: 'Get-ItemProperty',
      task: 'read registry values',
      syntax: "Get-ItemProperty -Path 'HKLM:\\SOFTWARE\\Microsoft\\Windows NT\\CurrentVersion' | Select-Object ProductName, DisplayVersion, CurrentBuild",
      effect: 'read',
      note: 'A 32-bit process reads the WOW6432Node view without saying so, which is why a script finds software in one PowerShell and not in another on the same machine.',
    },
    {
      name: 'Set-ItemProperty',
      task: 'write a registry value',
      syntax: "Set-ItemProperty -Path 'HKLM:\\SOFTWARE\\Example' -Name Setting -Value 1 -Type DWord",
      effect: 'changes',
      note: 'The -Type matters: writing a number without it creates a String, and whatever reads it expecting a DWord will not find it. It also fails if the key does not exist — New-Item first.',
    },
    {
      name: 'Get-Volume',
      task: 'list volumes and free space',
      syntax: "Get-Volume | Where-Object DriveLetter | Select-Object DriveLetter, FileSystemLabel, @{ n = 'FreeGB'; e = { [math]::Round($_.SizeRemaining / 1GB, 1) } }",
      effect: 'read',
      note: 'Simpler than Win32_LogicalDisk and it includes volumes with no drive letter, which is where mounted data volumes hide.',
    },
    {
      name: 'Get-Disk',
      task: 'list physical disks',
      syntax: 'Get-Disk | Select-Object Number, FriendlyName, Size, PartitionStyle, OperationalStatus, HealthStatus',
      effect: 'read',
      note: 'PartitionStyle RAW means it has never been initialised. HealthStatus is from the storage stack, not SMART — a disk can be Healthy here and failing.',
    },
    {
      name: 'Initialize-Disk',
      task: 'prepare a new disk',
      syntax: 'Get-Disk | Where-Object PartitionStyle -eq RAW | Initialize-Disk -PartitionStyle GPT -PassThru | New-Partition -AssignDriveLetter -UseMaximumSize | Format-Volume -FileSystem NTFS',
      effect: 'destructive',
      note: 'That one-liner is the standard new-disk incantation, and it will happily initialise a disk that only looks RAW because its controller is not loaded. Check Get-Disk output before piping.',
    },
    {
      name: 'Get-SmbShare',
      task: 'list file shares',
      syntax: 'Get-SmbShare | Where-Object Name -notlike "*$" | Get-SmbShareAccess',
      effect: 'read',
      note: 'Share and NTFS permissions combine, and the effective right is the more restrictive. Reading only one of them tells you half the story.',
    },
    {
      name: 'Get-SmbOpenFile',
      task: 'see who has a file open on a share',
      syntax: 'Get-SmbOpenFile | Where-Object Path -like "*\\Finance\\*" | Select-Object ClientUserName, ClientComputerName, Path',
      effect: 'read',
      note: 'This is the answer to "who has this open" without walking to someone\u2019s desk. Close-SmbOpenFile forces it shut, and loses whatever was unsaved.',
    },
    {
      name: 'Get-Partition',
      task: 'list partitions',
      syntax: 'Get-Partition | Select-Object DiskNumber, PartitionNumber, DriveLetter, Size, Type',
      effect: 'read',
      note: 'Resize-Partition can extend a volume live, but only into contiguous free space immediately after it. That constraint is why extending sometimes "is not possible" for no visible reason.',
    },
    {
      name: 'Get-NetFirewallRule',
      task: 'list firewall rules',
      syntax: "Get-NetFirewallRule -Enabled True -Direction Inbound | Get-NetFirewallPortFilter | Where-Object LocalPort -eq 3389",
      effect: 'read',
      note: 'The rule and its filters are separate objects, which is why finding "the rule for port 3389" needs two commands piped together. It is the least discoverable part of the module.',
    },
    {
      name: 'New-NetFirewallRule',
      task: 'add a firewall rule',
      syntax: 'New-NetFirewallRule -DisplayName "App 8080" -Direction Inbound -Protocol TCP -LocalPort 8080 -RemoteAddress 10.0.0.0/8, 2001:db8::/32 -Action Allow',
      effect: 'changes',
      note: 'Without -RemoteAddress the rule is open to everything that can reach the machine, over IPv4 and IPv6. Scope it — one rule takes networks of both families — and use -Profile to say which network profiles it applies to.',
    },
    {
      name: 'Get-Clipboard',
      task: 'read what is on the clipboard',
      syntax: 'Get-Clipboard | ConvertFrom-Csv',
      effect: 'read',
      note: 'Surprisingly useful for one-off work: paste a list of server names out of an email and pipe it straight into something.',
      availability: 'Windows only',
    },
  ]),

  group('powershell', 'Networking', [
    {
      name: 'Test-NetConnection',
      task: 'test whether a port is reachable, and trace the route',
      syntax: 'Test-NetConnection -ComputerName db01 -Port 1433 -InformationLevel Detailed',
      effect: 'read',
      note: 'The one command that answers "can this machine reach that port", with the route and the source interface. It is slow — a few seconds each — so it is wrong in a loop over hundreds of hosts.',
    },
    {
      name: 'Test-Connection',
      task: 'test whether a host answers, and how long it takes',
      syntax: 'Test-Connection -ComputerName SRV01 -Count 1 -Quiet -TimeoutSeconds 2',
      effect: 'read',
      note: '-Quiet returns a plain true or false, which is what a condition wants. In PowerShell 7 the parameters changed — -TimeoutSeconds does not exist in 5.1.',
    },
    {
      name: 'Resolve-DnsName',
      task: 'look up a DNS record from PowerShell',
      syntax: 'Resolve-DnsName -Name www.example.com -Type A -Server 8.8.8.8 -DnsOnly',
      effect: 'read',
      note: '-DnsOnly skips the local cache and the hosts file, which is what you want when checking what DNS actually says rather than what this machine believes.',
      availability: 'Windows only',
    },
    {
      name: 'Resolve-DnsName -Type AAAA',
      task: 'look up the IPv6 address of a name',
      syntax: 'Resolve-DnsName -Name www.example.com -Type AAAA -Server 2001:4860:4860::8888 -DnsOnly',
      effect: 'read',
      note: 'A dual-stack client tries the AAAA answer first. If a name works from one machine and hangs for twenty seconds from another, check whether it has an AAAA record pointing somewhere that does not answer.',
      availability: 'Windows only',
    },
    {
      name: 'Get-NetIPAddress',
      task: 'list IP addresses',
      syntax: 'Get-NetIPAddress -AddressFamily IPv4 | Where-Object PrefixOrigin -ne WellKnown | Select-Object InterfaceAlias, IPAddress, PrefixLength',
      effect: 'read',
      note: 'PrefixOrigin says whether an address came from DHCP or was set by hand, which is the thing you are usually trying to establish.',
      related: ['ps.get-netipaddress-addressfamily-ipv6'],
    },
    {
      name: 'Get-NetIPAddress -AddressFamily IPv6',
      task: 'list IPv6 addresses and where each one came from',
      syntax: "Get-NetIPAddress -AddressFamily IPv6 | Where-Object { $_.PrefixOrigin -ne 'WellKnown' -and $_.IPAddress -notlike 'fe80*' } | Select-Object InterfaceAlias, IPAddress, PrefixLength, PrefixOrigin, SuffixOrigin",
      effect: 'read',
      note: 'PrefixOrigin RouterAdvertisement with SuffixOrigin Random is a temporary privacy address that changes daily; the stable one has SuffixOrigin Link or Manual. Register and allow-list the stable one, never the temporary one.',
    },
    {
      name: 'Get-NetAdapter',
      task: 'list network adapters',
      syntax: 'Get-NetAdapter | Where-Object Status -eq Up | Select-Object Name, InterfaceDescription, LinkSpeed, MacAddress',
      effect: 'read',
      note: 'LinkSpeed as a string is "1 Gbps"; the numeric one is .Speed in bits. A 100 Mbps link on a server is almost always a duplex or cable fault worth chasing.',
    },
    {
      name: 'Get-NetRoute',
      task: 'show the routing table',
      syntax: 'Get-NetRoute -AddressFamily IPv4 | Sort-Object RouteMetric | Select-Object -First 20',
      effect: 'read',
      note: 'Find-NetRoute -RemoteIPAddress answers "which interface would this actually go out of", which is the question when a machine has several.',
    },
    {
      name: 'Get-NetRoute -AddressFamily IPv6',
      task: 'show the IPv6 default route and where it came from',
      syntax: 'Get-NetRoute -AddressFamily IPv6 -DestinationPrefix ::/0 | Select-Object InterfaceAlias, NextHop, RouteMetric, Protocol',
      effect: 'read',
      note: 'The next hop is a fe80:: link-local address, and that is correct — IPv6 routers are reached by link-local. Protocol RouterAdvertisement means the router handed it out; no ::/0 route at all means IPv6 only works on the local subnet.',
    },
    {
      name: 'Get-NetTCPConnection',
      task: 'list TCP connections and what is listening',
      syntax: "Get-NetTCPConnection -State Listen | Select-Object LocalAddress, LocalPort, @{ n = 'Process'; e = { (Get-Process -Id $_.OwningProcess).Name } }",
      effect: 'read',
      note: 'OwningProcess is a pid, not a name — joining it to Get-Process is what makes the output useful, and is the reason this is a two-part command in every script that uses it.',
    },
    {
      name: 'Invoke-WebRequest',
      task: 'fetch a web page or call an HTTP endpoint',
      syntax: 'Invoke-WebRequest -Uri https://api.example.com/health -TimeoutSec 10 -UseBasicParsing',
      effect: 'read',
      note: 'In Windows PowerShell 5.1 it uses Internet Explorer to parse the page unless you pass -UseBasicParsing, which fails on a server with IE never opened. It also throws on any non-2xx status, so wrap it.',
      related: ['ps.invoke-restmethod'],
    },
    {
      name: 'Invoke-RestMethod',
      task: 'call a REST API and get objects back',
      syntax: 'Invoke-RestMethod -Uri https://api.example.com/items -Headers @{ Authorization = "Bearer $token" } -Method Get',
      effect: 'changes',
      note: 'It parses JSON for you, which Invoke-WebRequest does not. It also hides the status code and the headers — use Invoke-WebRequest when you need those.',
    },
    {
      name: 'Start-BitsTransfer',
      task: 'download a large file, resumably',
      syntax: 'Start-BitsTransfer -Source https://example.com/big.iso -Destination D:\\big.iso -Priority Normal',
      effect: 'changes',
      note: 'It survives a disconnect and resumes, which Invoke-WebRequest does not. It runs as a service, so it keeps going after the session ends — and it does not work as SYSTEM in a scheduled task.',
      availability: 'Windows only',
    },
    {
      name: 'Get-DnsServerResourceRecord',
      task: 'read DNS records from a server',
      syntax: 'Get-DnsServerResourceRecord -ComputerName dc01 -ZoneName example.com -RRType A | Where-Object { $_.Timestamp -lt (Get-Date).AddDays(-30) }',
      module: 'DnsServer',
      effect: 'read',
      note: 'A record with no timestamp is static, and static records never scavenge. That is why a zone with scavenging on still fills up with stale entries.',
    },
    {
      name: 'Get-DnsServerResourceRecord -RRType AAAA',
      task: 'read IPv6 (AAAA) DNS records from a server',
      syntax: 'Get-DnsServerResourceRecord -ComputerName dc01 -ZoneName example.com -RRType AAAA | Select-Object HostName, Timestamp, @{ n = "Address"; e = { $_.RecordData.IPv6Address } }',
      module: 'DnsServer',
      effect: 'read',
      note: 'Reports that only ask for -RRType A never see these, so stale AAAA records outlive the machines they pointed at — and dual-stack clients try them first.',
    },
    {
      name: 'Get-DhcpServerv4Scope',
      task: 'list DHCP scopes',
      syntax: 'Get-DhcpServerv4Scope -ComputerName dhcp01 | Get-DhcpServerv4ScopeStatistics | Where-Object PercentageInUse -gt 85',
      module: 'DhcpServer',
      effect: 'read',
      note: 'Scope utilisation is the number that predicts an outage. A scope at 95% on a Friday is a Monday morning where new machines get no address.',
    },
  ]),

  group('powershell', 'Security and certificates', [
    {
      name: 'Get-ChildItem Cert:',
      task: 'list certificates in a store',
      syntax: 'Get-ChildItem Cert:\\LocalMachine\\My | Where-Object NotAfter -lt (Get-Date).AddDays(45) | Select-Object Subject, NotAfter, Thumbprint',
      effect: 'read',
      note: 'The certificate store is a PowerShell drive, so everything that works on files works here. -Recurse across Cert:\\LocalMachine lists every store at once.',
    },
    {
      name: 'New-SelfSignedCertificate',
      task: 'make a self-signed certificate',
      syntax: 'New-SelfSignedCertificate -DnsName app.internal -CertStoreLocation Cert:\\LocalMachine\\My -NotAfter (Get-Date).AddYears(2)',
      effect: 'changes',
      note: 'Fine for a lab and for internal service-to-service where both ends are configured. Anything a browser sees needs a real certificate — a self-signed one teaches users to click through warnings.',
    },
    {
      name: 'Export-PfxCertificate',
      task: 'export a certificate with its private key',
      syntax: 'Export-PfxCertificate -Cert Cert:\\LocalMachine\\My\\$thumbprint -FilePath app.pfx -Password $securePassword',
      effect: 'changes',
      note: 'The PFX contains the private key. Treat the file and its password as the key itself — a PFX in a share or a ticket is a compromised certificate.',
    },
    {
      name: 'Get-AuthenticodeSignature',
      task: 'check whether a file is signed, and by whom',
      syntax: 'Get-ChildItem *.ps1 | Get-AuthenticodeSignature | Where-Object Status -ne Valid',
      effect: 'read',
      note: 'Status Valid means the signature checks out; NotSigned and HashMismatch are both worth investigating. A HashMismatch means the file changed after signing.',
    },
    {
      name: 'ConvertTo-SecureString',
      task: 'turn text into a SecureString',
      syntax: 'ConvertTo-SecureString -String $plain -AsPlainText -Force',
      effect: 'read',
      note: 'Using this on a literal in a script defeats the entire point — the password is in the file. It is for converting something you just generated or just prompted for.',
    },
    {
      name: 'Protect-CmsMessage',
      task: 'encrypt text to a certificate',
      syntax: 'Protect-CmsMessage -To $thumbprint -Content $secret -OutFile secret.cms',
      effect: 'changes',
      note: 'Proper certificate-based encryption that works across machines, unlike ConvertFrom-SecureString which is tied to the user and machine that created it.',
    },
    {
      name: 'Get-ExecutionPolicy',
      task: 'check the script execution policy',
      syntax: 'Get-ExecutionPolicy -List',
      effect: 'read',
      note: 'It is not a security boundary — anyone can bypass it with -ExecutionPolicy Bypass or by piping the script to PowerShell. It stops accidents, not attackers. -List shows all five scopes, which is how you find out which one is winning.',
    },
    {
      name: 'Get-LocalUser',
      task: 'list local accounts',
      syntax: 'Get-LocalUser | Where-Object Enabled | Select-Object Name, LastLogon, PasswordLastSet, PasswordExpires',
      effect: 'read',
      note: 'PasswordLastSet being null means the password has never been set. An enabled account with no password set and no last logon is usually left over from imaging.',
    },
    {
      name: 'Get-CimInstance Win32_Service (accounts)',
      task: 'find which accounts services run as',
      syntax: 'Get-CimInstance Win32_Service | Where-Object { $_.StartName -notlike "*LocalSystem" -and $_.StartName -notlike "NT *" } | Select-Object Name, StartName, PathName',
      effect: 'read',
      note: 'A service running as a domain account is a stored password that never expires and never rotates. Finding them all is the first step in moving to group managed service accounts.',
    },
    {
      name: 'Get-WinEvent Security 4625',
      task: 'find failed logons',
      syntax: "Get-WinEvent -FilterHashtable @{ LogName = 'Security'; Id = 4625; StartTime = (Get-Date).AddHours(-24) } | Group-Object { $_.Properties[5].Value } | Sort-Object Count -Descending",
      effect: 'read',
      note: 'The interesting fields are positional in .Properties, not named — index 5 is the account name and 19 is the source address. That indexing is why this is always copied rather than written.',
    },
    {
      name: 'Get-SmbServerConfiguration',
      task: 'check SMB settings, including SMBv1',
      syntax: 'Get-SmbServerConfiguration | Select-Object EnableSMB1Protocol, EnableSMB2Protocol, RequireSecuritySignature',
      effect: 'read',
      note: 'SMBv1 enabled is a finding in any audit and the path several ransomware families used. Disabling it breaks very old devices — scanners and industrial equipment, usually.',
    },
  ]),

  group('powershell', 'Microsoft 365 and Entra', [
    {
      name: 'Connect-MgGraph',
      task: 'sign in to Microsoft Graph',
      syntax: "Connect-MgGraph -Scopes 'User.Read.All', 'Directory.Read.All' -NoWelcome",
      module: 'Microsoft.Graph',
      effect: 'changes',
      note: 'Scopes are per session and consented once per application. A script that fails with "insufficient privileges" usually asked for the wrong scope, not the wrong permission — Get-MgContext shows what it actually has.',
    },
    {
      name: 'Get-MgUser',
      task: 'list or find users in Entra ID',
      syntax: 'Get-MgUser -All -Property Id, DisplayName, UserPrincipalName, AccountEnabled, SignInActivity',
      module: 'Microsoft.Graph.Users',
      effect: 'read',
      note: 'Without -All you get the first hundred and no indication there were more. SignInActivity needs an Entra ID P1 licence and comes back empty without one — silently.',
    },
    {
      name: 'Get-MgUserAuthenticationMethod',
      task: 'see what second factors a user has registered',
      syntax: 'Get-MgUserAuthenticationMethod -UserId $id',
      module: 'Microsoft.Graph.Identity.SignIns',
      effect: 'read',
      note: 'The type is in AdditionalProperties["@odata.type"], not a named property, so reading it takes an awkward line. A password method on its own means no second factor.',
    },
    {
      name: 'Get-MgDirectoryRole',
      task: 'list active directory roles',
      syntax: 'Get-MgDirectoryRole -All | ForEach-Object { Get-MgDirectoryRoleMember -DirectoryRoleId $_.Id -All }',
      module: 'Microsoft.Graph.Identity.DirectoryManagement',
      effect: 'read',
      note: 'It only lists roles that have been activated in the tenant, not every role that exists. A role with no members may simply never have been activated.',
    },
    {
      name: 'Connect-ExchangeOnline',
      task: 'sign in to Exchange Online',
      syntax: 'Connect-ExchangeOnline -ShowBanner:$false',
      module: 'ExchangeOnlineManagement',
      effect: 'changes',
      note: 'Always Disconnect-ExchangeOnline in a finally block. An abandoned session counts against the tenant concurrency limit until it times out, and eventually blocks new connections for everyone.',
    },
    {
      name: 'Get-EXOMailbox',
      task: 'list mailboxes, fast',
      syntax: 'Get-EXOMailbox -RecipientTypeDetails UserMailbox -ResultSize Unlimited -Properties ForwardingSmtpAddress',
      module: 'ExchangeOnlineManagement',
      effect: 'read',
      note: 'The EXO cmdlets are the fast REST-based ones; Get-Mailbox is the old remote PowerShell version and is far slower on a large tenant. Without -ResultSize Unlimited you get a thousand.',
    },
    {
      name: 'Get-EXOMailboxStatistics',
      task: 'mailbox size and last logon',
      syntax: 'Get-EXOMailboxStatistics -Identity $id -Properties LastLogonTime',
      module: 'ExchangeOnlineManagement',
      effect: 'read',
      note: 'One throttled call per mailbox. On ten thousand mailboxes this is hours, and the throttling is invisible — the script just gets slower. TotalItemSize is a string like "12.3 GB (13,251,212,182 bytes)"; only the bracketed number sorts.',
    },
    {
      name: 'Get-InboxRule',
      task: 'list a mailbox\u2019s inbox rules',
      syntax: 'Get-InboxRule -Mailbox user@example.com | Where-Object { $_.ForwardTo -or $_.RedirectTo }',
      module: 'ExchangeOnlineManagement',
      effect: 'read',
      note: 'A compromised mailbox is usually forwarded by a rule rather than by the mailbox property, because the property is the one an administrator would look at. Checking both is the point.',
    },
    {
      name: 'Get-MessageTrace',
      task: 'trace where a message went',
      syntax: 'Get-MessageTrace -SenderAddress user@example.com -StartDate (Get-Date).AddDays(-2) -EndDate (Get-Date)',
      module: 'ExchangeOnlineManagement',
      effect: 'read',
      note: 'Only covers the last ten days. Beyond that it is the historical search, which is asynchronous and arrives by email hours later.',
    },
    {
      name: 'Get-MgAuditLogSignIn',
      task: 'read sign-in logs',
      syntax: "Get-MgAuditLogSignIn -Filter \"createdDateTime ge $($since.ToString('o'))\" -Top 1000",
      module: 'Microsoft.Graph.Reports',
      effect: 'read',
      note: 'Needs AuditLog.Read.All and an Entra ID P1 licence. The filter syntax is OData, not PowerShell — dates have to be ISO 8601 strings and the operators are ge and le.',
    },
  ]),

  group('powershell', 'Azure', [
    {
      name: 'Connect-AzAccount',
      task: 'sign in to Azure',
      syntax: 'Connect-AzAccount -Identity   # a managed identity, for running in Azure',
      module: 'Az.Accounts',
      effect: 'changes',
      note: 'A managed identity has no password and cannot be phished. It is the right answer for anything running in Azure, and it is one parameter.',
    },
    {
      name: 'Set-AzContext',
      task: 'choose which subscription commands run against',
      syntax: 'Set-AzContext -SubscriptionId $id',
      module: 'Az.Accounts',
      effect: 'changes',
      note: 'The context is process-wide. A script that forgets to set it runs against whichever subscription was last used — which is how a change lands in production instead of test.',
    },
    {
      name: 'Get-AzResource',
      task: 'list resources',
      syntax: 'Get-AzResource -ResourceGroupName rg-prod -ResourceType Microsoft.Compute/virtualMachines',
      module: 'Az.Resources',
      effect: 'read',
      note: 'Across many subscriptions this is slow. Search-AzGraph queries the Resource Graph and answers in seconds what this takes minutes to do, with a real query language.',
      related: ['ps.search-azgraph'],
    },
    {
      name: 'Search-AzGraph',
      task: 'query every resource across every subscription, fast',
      syntax: "Search-AzGraph -Query \"Resources | where type =~ 'microsoft.compute/virtualmachines' | project name, location, tags\" -First 1000",
      module: 'Az.ResourceGraph',
      effect: 'read',
      note: 'The right tool for any estate-wide question. It pages at 1000 by default and you have to ask for the rest with -SkipToken, which is the mistake everyone makes once.',
    },
    {
      name: 'Update-AzTag',
      task: 'add or replace tags on a resource',
      syntax: 'Update-AzTag -ResourceId $id -Tag @{ CostCentre = "1234" } -Operation Merge',
      module: 'Az.Resources',
      effect: 'changes',
      note: 'Merge adds without touching the others; Replace wipes everything not in the hashtable. Using Replace by accident is how a governance script removes every tag something else set.',
    },
    {
      name: 'Get-AzVM',
      task: 'list virtual machines',
      syntax: 'Get-AzVM -Status | Select-Object Name, ResourceGroupName, PowerState, @{ n = "Size"; e = { $_.HardwareProfile.VmSize } }',
      module: 'Az.Compute',
      effect: 'read',
      note: 'Without -Status there is no PowerState at all, and with it the call is much slower. A stopped-but-not-deallocated VM still bills for compute — the state to look for is "VM deallocated".',
    },
    {
      name: 'Get-AzRoleAssignment',
      task: 'see who has access to what',
      syntax: 'Get-AzRoleAssignment -Scope "/subscriptions/$id" | Where-Object RoleDefinitionName -eq Owner',
      module: 'Az.Resources',
      effect: 'read',
      note: 'Assignments inherit down, so a role at the subscription applies to every resource in it. Without -Scope you get assignments at and below the current context, which is rarely what people expect.',
    },
    {
      name: 'Get-AzActivityLog',
      task: 'find who changed what',
      syntax: 'Get-AzActivityLog -StartTime (Get-Date).AddDays(-7) -ResourceGroupName rg-prod | Where-Object { $_.OperationName.Value -like "*write*" }',
      module: 'Az.Monitor',
      effect: 'read',
      note: 'Only keeps 90 days, and the module caps a query at 15 days at a time. For anything longer, the log has to be exported to a workspace or storage first.',
    },
    {
      name: 'Get-AzStorageAccount',
      task: 'list storage accounts and their settings',
      syntax: 'Get-AzStorageAccount | Select-Object StorageAccountName, ResourceGroupName, EnableHttpsTrafficOnly, AllowBlobPublicAccess, MinimumTlsVersion',
      module: 'Az.Storage',
      effect: 'read',
      note: 'AllowBlobPublicAccess true is the setting behind most "open storage account" findings. It permits anonymous access to be configured per container — it does not on its own make anything public.',
    },
    {
      name: 'Get-AzKeyVaultSecret',
      task: 'read a secret from Key Vault',
      syntax: 'Get-AzKeyVaultSecret -VaultName kv-prod -Name db-password -AsPlainText',
      module: 'Az.KeyVault',
      effect: 'read',
      note: '-AsPlainText returns a string rather than a SecureString, which is convenient and means the value is in memory and possibly in a transcript. Reading a secret is logged in the vault\u2019s audit log, which is the point of using one.',
    },
    {
      name: 'Get-AzMetric',
      task: 'read metrics for a resource',
      syntax: "Get-AzMetric -ResourceId $id -MetricName 'Percentage CPU' -TimeGrain 01:00:00 -StartTime (Get-Date).AddDays(-7)",
      module: 'Az.Monitor',
      effect: 'read',
      note: 'Platform metrics are retained for 93 days at the finest grain and aggregate as they age. Asking for one-minute data from two months ago silently returns hourly.',
    },
  ]),

  group('powershell', 'VMware PowerCLI', [
    {
      name: 'Connect-VIServer',
      task: 'connect to vCenter',
      syntax: 'Connect-VIServer -Server vcenter01 -Credential $credential',
      module: 'VMware.PowerCLI',
      effect: 'changes',
      note: 'Set-PowerCLIConfiguration -ParticipateInCEIP $false -InvalidCertificateAction Ignore first, or a scheduled run blocks for ever on a prompt nobody answers. Always Disconnect-VIServer in a finally block.',
    },
    {
      name: 'Get-VM',
      task: 'list virtual machines',
      syntax: 'Get-VM | Where-Object PowerState -eq PoweredOn | Select-Object Name, NumCpu, MemoryGB, @{ n = "UsedGB"; e = { [math]::Round($_.UsedSpaceGB, 1) } }',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'ProvisionedSpaceGB and UsedSpaceGB differ by the whole thin-provisioning story. .ExtensionData is the raw managed object and holds everything the cmdlet does not surface.',
    },
    {
      name: 'Get-Snapshot',
      task: 'find snapshots',
      syntax: 'Get-VM | Get-Snapshot | Where-Object Created -lt (Get-Date).AddDays(-7) | Sort-Object SizeGB -Descending',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'Old snapshots are the single most common cause of a full datastore. A snapshot is not a backup: it is a delta that grows without limit and slows every write.',
    },
    {
      name: 'Remove-Snapshot',
      task: 'delete a snapshot and consolidate it',
      syntax: 'Get-VM web01 | Get-Snapshot -Name "Before patch" | Remove-Snapshot -Confirm:$false',
      module: 'VMware.PowerCLI',
      effect: 'destructive',
      note: 'Consolidation is heavy I/O for as long as it takes — hours on a large snapshot — and the VM can be unresponsive. Check the datastore has free space at least the size of the snapshot first.',
    },
    {
      name: 'Get-Stat',
      task: 'read performance statistics',
      syntax: 'Get-Stat -Entity $vms -Stat cpu.usage.average -Start (Get-Date).AddDays(-7) -IntervalMins 120',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'Pass every entity in one call. One call per VM goes back to vCenter each time and turns a minute into an hour. The rolled-up intervals are far faster than real-time.',
    },
    {
      name: 'Get-VMHost',
      task: 'list ESXi hosts',
      syntax: 'Get-VMHost | Select-Object Name, ConnectionState, PowerState, NumCpu, MemoryTotalGB, Version, Build',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'Build matters more than Version for patch compliance — two hosts on 8.0.2 can be months apart. ConnectionState NotResponding is different from Disconnected and means something different.',
    },
    {
      name: 'Get-Datastore',
      task: 'list datastores and free space',
      syntax: 'Get-Datastore | Where-Object { $_.FreeSpaceGB / $_.CapacityGB -lt 0.15 } | Sort-Object FreeSpaceGB',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'A datastore filling up stops every VM on it at once, which is the worst failure mode in the environment. Over-provisioning plus snapshots is how it happens.',
    },
    {
      name: 'New-VM',
      task: 'create a virtual machine',
      syntax: 'New-VM -Name app01 -Template $template -ResourcePool $cluster -Datastore $datastore -OSCustomizationSpec $spec',
      module: 'VMware.PowerCLI',
      effect: 'changes',
      note: 'From a template with a customisation spec is the only version worth scripting. Without the spec the clone comes up with the template\u2019s name and SID, on the network, which causes problems immediately.',
    },
    {
      name: 'Get-VMHostNetworkAdapter',
      task: 'list host VMkernel and physical adapters',
      syntax: 'Get-VMHost | Get-VMHostNetworkAdapter | Where-Object { $_.VMKernel } | Select-Object VMHost, Name, IP, Mtu',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'An MTU mismatch between the VMkernel port, the switch and the physical network is the classic vMotion or vSAN fault — it works until a full-size frame is sent.',
    },
    {
      name: 'Get-VIEvent',
      task: 'read vCenter events — who did what',
      syntax: 'Get-VIEvent -Entity (Get-VM web01) -Start (Get-Date).AddDays(-7) -MaxSamples 500',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'The answer to "who powered this off". Without -MaxSamples it defaults to 100 and quietly truncates, which is how people conclude nothing happened.',
    },
    {
      name: 'Get-TagAssignment',
      task: 'read vSphere tags',
      syntax: 'Get-VM | Get-TagAssignment -Category Environment',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'Tags live in the inventory service, not in vCenter proper, so they survive a VM being moved between clusters and are the sane way to drive automation.',
    },
  ]),

  group('powershell', 'Modules and packages', [
    {
      name: 'Find-Module',
      task: 'search the PowerShell Gallery',
      syntax: 'Find-Module -Name Az.* -Repository PSGallery | Select-Object Name, Version, Author',
      effect: 'read',
      note: 'Anyone can publish to the Gallery. Check the author and the download count before installing anything that will run with privilege.',
    },
    {
      name: 'Install-Module',
      task: 'install a module',
      syntax: 'Install-Module -Name Microsoft.Graph -Scope CurrentUser -Repository PSGallery -Force',
      effect: 'changes',
      note: '-Scope CurrentUser avoids needing administrator and avoids affecting everyone on the machine. -AllowClobber is sometimes needed and is a warning sign: something already provides those commands.',
    },
    {
      name: 'Get-Module',
      task: 'see what is loaded or available',
      syntax: 'Get-Module -ListAvailable | Group-Object Name | Where-Object Count -gt 1',
      effect: 'read',
      note: 'That one-liner finds modules installed more than once at different versions, which is the cause of "it works on my machine" more often than anything else in PowerShell.',
    },
    {
      name: 'Import-Module',
      task: 'load a module explicitly',
      syntax: 'Import-Module ActiveDirectory -ErrorAction Stop',
      effect: 'changes',
      note: 'Modules autoload on first use, so this is only needed to force a specific version, to fail early with a clear message, or in a constrained environment where autoloading is off.',
    },
    {
      name: 'Update-Module',
      task: 'update an installed module',
      syntax: 'Update-Module -Name Az -Force',
      effect: 'changes',
      note: 'It installs the new version alongside the old rather than replacing it. Over time that is a lot of disk and a lot of ambiguity — Uninstall-Module -AllVersions then reinstall is the tidy path.',
    },
    {
      name: 'Save-Module',
      task: 'download a module without installing it',
      syntax: 'Save-Module -Name Az -Path .\\offline-modules -Repository PSGallery',
      effect: 'changes',
      note: 'This is how you get modules onto an air-gapped machine: save on a connected one, copy the folder to a module path on the other.',
    },
    {
      name: 'Register-PSRepository',
      task: 'add an internal module repository',
      syntax: 'Register-PSRepository -Name Internal -SourceLocation https://nuget.example.com/v2 -InstallationPolicy Trusted',
      effect: 'changes',
      note: 'Trusted stops the confirmation prompt on every install, which is what makes an internal repository usable from a script.',
    },
    {
      name: 'Invoke-ScriptAnalyzer',
      task: 'lint a script',
      syntax: 'Invoke-ScriptAnalyzer -Path .\\script.ps1 -Severity Warning, Error -Recurse',
      module: 'PSScriptAnalyzer',
      effect: 'read',
      note: 'It catches the things that are legal and wrong: aliases in a script, Write-Host where output was meant, an unapproved verb, a plain-text password parameter.',
    },
    {
      name: 'Invoke-Pester',
      task: 'run the Pester test suite',
      syntax: 'Invoke-Pester -Path .\\Tests -Output Detailed',
      module: 'Pester',
      effect: 'read',
      note: 'Pester 5 changed the syntax enough that Pester 4 tests do not run on it. Check which version is installed before wondering why a working test suite stopped working.',
    },
  ]),
];
