/**
 * PowerShell, part two: the language itself and the rest of the estate.
 *
 * The first file covers the pipeline, files, Active Directory, Azure and the
 * things most scripts touch. This one covers what people reach for after that —
 * the language constructs that separate a script from a one-liner, and the
 * server roles that are still most of a Windows estate: Exchange, SQL Server,
 * Hyper-V, DNS and DHCP, clustering, IIS, printing, patching and Defender.
 *
 * Same rule as everywhere else in the catalogue: the syntax is a real
 * invocation, and the note is the thing that is true, not obvious, and costs an
 * afternoon. An entry without one of those is a worse version of Get-Help.
 */

import { group,                   } from './catalog.js';

export const POWERSHELL_CATALOG_2                          = [
  group('powershell', 'The language itself', [
    {
      name: '@splat',
      task: 'pass a hashtable of parameters instead of a very long line',
      syntax: "$params = @{\n    Name        = 'svc-app'\n    Path        = 'OU=Service,DC=example,DC=com'\n    Enabled     = $true\n    ErrorAction = 'Stop'\n}\nNew-ADUser @params",
      effect: 'read',
      note: 'The splat uses @ where the variable would use $. It is also the only clean way to build a call conditionally — add a key to the hashtable when a value is present, rather than writing four near-identical command lines behind if statements.',
      related: ['ps.cmdletbinding'],
    },
    {
      name: 'try/catch/finally',
      task: 'handle an error instead of stopping the whole script',
      syntax: "try {\n    Get-ADUser -Identity $name -ErrorAction Stop\n} catch [Microsoft.ActiveDirectory.Management.ADIdentityNotFoundException] {\n    Write-Warning \"$name does not exist\"\n} catch {\n    throw\n} finally {\n    Disconnect-VIServer -Confirm:$false -ErrorAction SilentlyContinue\n}",
      effect: 'read',
      note: 'catch only ever fires on a terminating error. Most cmdlets produce non-terminating ones, so without -ErrorAction Stop (or $ErrorActionPreference = "Stop") the catch block is dead code and the script carries on past the failure.',
      related: ['ps.erroractionpreference', 'ps.throw'],
    },
    {
      name: '$ErrorActionPreference',
      task: 'make errors stop the script by default',
      syntax: "$ErrorActionPreference = 'Stop'",
      effect: 'read',
      note: 'The single line that turns PowerShell from "carries on after a failure" into "stops". It does not affect native executables at all — those set $LASTEXITCODE, which you still have to check yourself.',
      related: ['ps.lastexitcode'],
    },
    {
      name: '$LASTEXITCODE',
      task: 'check whether an external program succeeded',
      syntax: "& robocopy $src $dst /E\nif ($LASTEXITCODE -ge 8) { throw \"robocopy failed with $LASTEXITCODE\" }",
      effect: 'read',
      note: '$? and $ErrorActionPreference tell you nothing about an exe. Only $LASTEXITCODE does, and it is overwritten by the next external call, so test it on the very next line.',
    },
    {
      name: 'throw',
      task: 'stop with a message the caller can catch',
      syntax: "throw [System.IO.FileNotFoundException]::new(\"config missing: $path\")",
      effect: 'read',
      note: 'throw always terminates; Write-Error does not unless -ErrorAction Stop is set. Throwing a typed exception lets the caller catch that one kind rather than catching everything and guessing.',
    },
    {
      name: '[CmdletBinding()]',
      task: 'turn a function into a proper cmdlet',
      syntax: "function Remove-StaleUser {\n    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = 'High')]\n    param(\n        [Parameter(Mandatory, ValueFromPipeline)][string]$Identity\n    )\n    process {\n        if ($PSCmdlet.ShouldProcess($Identity, 'Disable')) { Disable-ADAccount -Identity $Identity }\n    }\n}",
      effect: 'read',
      note: 'One attribute gives you -Verbose, -Debug, -ErrorAction and, with SupportsShouldProcess, -WhatIf and -Confirm for free. ConfirmImpact High means it prompts even without -Confirm, which is what you want on anything destructive.',
      related: ['ps.pscmdlet-shouldprocess', 'ps.param-validation'],
    },
    {
      name: '$PSCmdlet.ShouldProcess',
      task: 'give a function a working -WhatIf',
      syntax: "if ($PSCmdlet.ShouldProcess($target, 'Delete snapshot')) {\n    Remove-Snapshot -Snapshot $snap -Confirm:$false\n}",
      effect: 'read',
      note: 'This is PowerShell\u2019s dry run and it is free — but only if every change in the function is inside one of these. A single unguarded change makes -WhatIf a lie, which is worse than not offering it.',
    },
    {
      name: 'param validation',
      task: 'reject bad input before the function runs',
      syntax: "param(\n    [ValidateSet('dev','test','prod')][string]$Environment,\n    [ValidateRange(1, 365)][int]$Days = 30,\n    [ValidatePattern('^[a-z0-9-]{3,32}$')][string]$Name,\n    [ValidateScript({ Test-Path $_ -PathType Leaf })][string]$ConfigFile\n)",
      effect: 'read',
      note: 'ValidateSet also gives tab completion, which is half the reason to use it. ValidateScript must return a boolean — a script block that outputs anything else passes by accident, and one that throws gives a clearer message than one that returns $false.',
    },
    {
      name: 'begin/process/end',
      task: 'write a function that streams the pipeline',
      syntax: "function Test-Host {\n    param([Parameter(ValueFromPipeline)][string]$Name)\n    begin { $results = @() }\n    process { $results += [pscustomobject]@{ Name = $Name; Up = Test-Connection $Name -Count 1 -Quiet } }\n    end { $results }\n}",
      effect: 'read',
      note: 'Without a process block, a pipeline-bound parameter only ever holds the last item — the function runs once, not once per item. That is the commonest reason a "working" function silently processes one of five hundred servers.',
    },
    {
      name: '[pscustomobject]',
      task: 'build an object with the columns you want',
      syntax: "[pscustomobject]@{\n    Name    = $vm.Name\n    CPU     = $vm.NumCpu\n    MemoryGB = $vm.MemoryGB\n    Owner   = $vm.CustomFields['Owner']\n}",
      effect: 'read',
      note: 'Property order is preserved with [pscustomobject] and is not with New-Object PSObject -Property, which sorts them arbitrarily. Objects, not formatted strings, are what makes the output usable by Export-Csv and everything downstream.',
      related: ['ps.select-object'],
    },
    {
      name: '-match and $Matches',
      task: 'extract part of a string with a regular expression',
      syntax: "if ($line -match '^(?<time>\\S+)\\s+(?<level>WARN|ERROR)\\s+(?<msg>.*)$') {\n    $Matches.level\n    $Matches.msg\n}",
      effect: 'read',
      note: '$Matches keeps whatever the last successful match put there, so testing it after a failed -match reads the previous match\u2019s data. Always use it inside the if block that succeeded, never after it.',
    },
    {
      name: '$null comparison',
      task: 'test for null without being caught out',
      syntax: "if ($null -eq $value) { ... }",
      effect: 'read',
      note: 'Put $null on the left. With it on the right, comparing against an array filters the array instead of returning a boolean — @(1,$null,2) -eq $null returns one element, which is truthy, so the test passes when it should fail.',
    },
    {
      name: '-as and -is',
      task: 'convert or test a type without throwing',
      syntax: "$port = $text -as [int]\nif ($null -eq $port) { throw \"not a number: $text\" }",
      effect: 'read',
      note: '-as returns $null on failure where a cast throws, which makes it the right tool for validating input. [int]"08" works, unlike the same thing in batch, but [int]"8.6" rounds to 9 rather than truncating.',
    },
    {
      name: 'here-string',
      task: 'write a block of text with quotes and newlines in it',
      syntax: "$body = @\"\nHost: $name\nStatus: $status\n\"@",
      effect: 'read',
      note: 'The closing @" or @\u2019 must be at the very start of its line with nothing before it, not even a space — the error when it is indented points at the wrong line entirely. Single-quoted @\u2019 does not expand variables, which is what you want for a template.',
    },
    {
      name: '#Requires',
      task: 'refuse to run in the wrong environment',
      syntax: "#Requires -Version 7.0\n#Requires -Modules ActiveDirectory, @{ ModuleName = 'Az.Accounts'; ModuleVersion = '2.12.0' }\n#Requires -RunAsAdministrator",
      effect: 'read',
      note: 'It fails before a single line runs, which is the difference between "you are not an administrator" and a script that gets halfway through and leaves the job in pieces. It must be at the top, and it is ignored inside a function.',
    },
    {
      name: 'class',
      task: 'define a type with validation and methods',
      syntax: "class Host {\n    [string]$Name\n    [int]$Port = 443\n    [bool] Reachable() { return (Test-NetConnection $this.Name -Port $this.Port -InformationLevel Quiet) }\n}",
      effect: 'read',
      note: 'Classes are parsed at compile time, so a class in a script that is dot-sourced twice conflicts with itself, and using-statements must be the first thing in the file. For most work a [pscustomobject] is enough and has none of that.',
    },
    {
      name: 'ForEach-Object vs foreach',
      task: 'choose between the pipeline and the statement',
      syntax: "foreach ($item in $collection) { ... }      # faster, needs it all in memory\n$collection | ForEach-Object { ... }        # streams, can be piped further",
      effect: 'read',
      note: 'The foreach statement is several times faster but loads the whole collection first, which matters at a million rows. Inside the statement, break and continue work; inside ForEach-Object they do not do what you expect.',
    },
    {
      name: 'Set-PSDebug',
      task: 'trace a script line by line',
      syntax: 'Set-PSDebug -Trace 1',
      effect: 'read',
      note: 'Trace 1 prints each line as it runs, Trace 2 adds variable assignments. Turn it off with -Off; left on in a profile it makes every session unusable and is oddly hard to notice.',
    },
  ]),

  group('powershell', 'Exchange Online and Teams', [
    {
      name: 'Get-EXORecipient',
      task: 'find any mail-enabled object quickly',
      syntax: "Get-EXORecipient -ResultSize Unlimited -Properties RecipientTypeDetails | Group-Object RecipientTypeDetails -NoElement",
      module: 'ExchangeOnlineManagement',
      effect: 'read',
      note: 'The EXO cmdlets use the REST API and are an order of magnitude faster than the old remote-PowerShell ones; the catch is that they return a reduced property set unless -Properties names what you want.',
      related: ['ps.get-exomailboxpermission'],
    },
    {
      name: 'Get-EXOMailboxPermission',
      task: 'see who has access to a mailbox',
      syntax: "Get-EXOMailboxPermission -Identity user@example.com | Where-Object { -not $_.IsInherited -and $_.User -notlike 'NT AUTHORITY*' }",
      module: 'ExchangeOnlineManagement',
      effect: 'read',
      note: 'Full Access lives here, but Send As is in Get-RecipientPermission and Send On Behalf is a property of the mailbox itself. Three different cmdlets for one question, which is why audits miss two of them.',
    },
    {
      name: 'Set-Mailbox',
      task: 'change a mailbox setting',
      syntax: "Set-Mailbox -Identity user@example.com -LitigationHoldEnabled $true -LitigationHoldDuration 2555",
      module: 'ExchangeOnlineManagement',
      effect: 'changes',
      note: 'Changes can take up to an hour to show up in other cmdlets because of directory replication, so a read-back straight afterwards often shows the old value. Do not conclude it failed and run it again.',
    },
    {
      name: 'New-MailboxExportRequest',
      task: 'export a mailbox to a PST',
      syntax: "New-MailboxExportRequest -Mailbox user@example.com -FilePath '\\\\server\\pst$\\user.pst'",
      effect: 'changes',
      availability: 'Exchange on-premises only; Exchange Online has no PST export cmdlet.',
      note: 'It needs the Mailbox Import Export role, which is not in Organization Management by default. Completed requests stay in the queue and block a second export until removed with Remove-MailboxExportRequest.',
    },
    {
      name: 'Search-UnifiedAuditLog',
      task: 'find out who did something in Microsoft 365',
      syntax: "Search-UnifiedAuditLog -StartDate (Get-Date).AddDays(-7) -EndDate (Get-Date) -Operations HardDelete,SoftDelete -ResultSize 5000",
      module: 'ExchangeOnlineManagement',
      effect: 'read',
      note: 'It returns at most 5000 rows per call and pages by SessionId with -SessionCommand ReturnLargeSet — without that you silently get a slice. The AuditData column is JSON, so ConvertFrom-Json it before trying to filter.',
    },
    {
      name: 'Get-OrganizationConfig',
      task: 'read tenant-wide Exchange settings',
      syntax: "Get-OrganizationConfig | Select-Object Name, IsDehydrated, OAuth2ClientProfileEnabled",
      module: 'ExchangeOnlineManagement',
      effect: 'read',
      note: 'IsDehydrated true on a new tenant means many settings are not writable yet — Enable-OrganizationCustomization has to run once, and it can take hours to take effect.',
    },
    {
      name: 'Get-CsOnlineUser',
      task: 'see a user\u2019s Teams voice and policy assignment',
      syntax: "Get-CsOnlineUser -Identity user@example.com | Select-Object UserPrincipalName, EnterpriseVoiceEnabled, LineUri, TeamsMeetingPolicy",
      module: 'MicrosoftTeams',
      effect: 'read',
      note: 'Recent module versions moved most properties behind -Filter rather than client-side Where-Object, and changed several property names outright. A script written against an older version fails with nulls rather than errors.',
    },
    {
      name: 'Grant-CsTeamsMeetingPolicy',
      task: 'assign a Teams policy to a user or a group',
      syntax: "Grant-CsTeamsMeetingPolicy -Identity user@example.com -PolicyName 'Restricted'",
      module: 'MicrosoftTeams',
      effect: 'changes',
      note: 'Passing -PolicyName $null removes the assignment and returns the user to the global policy, which is the documented way to undo this. Group-based assignment can take 24 hours to apply and does not report progress.',
    },
    {
      name: 'Get-Team',
      task: 'list Teams and their owners',
      syntax: "Get-Team | ForEach-Object { [pscustomobject]@{ Name = $_.DisplayName; Owners = (Get-TeamUser -GroupId $_.GroupId -Role Owner).User -join ';' } }",
      module: 'MicrosoftTeams',
      effect: 'read',
      note: 'A team with no owner cannot be managed by anyone and is created every time the only owner is deleted. That one-liner is the report nobody runs until an audit asks for it.',
    },
    {
      name: 'Get-PnPList',
      task: 'work with SharePoint Online lists and libraries',
      syntax: "Connect-PnPOnline -Url https://contoso.sharepoint.com/sites/ops -Interactive\nGet-PnPList | Where-Object { $_.ItemCount -gt 5000 }",
      module: 'PnP.PowerShell',
      effect: 'read',
      note: 'The 5000-item list view threshold is what that filter is looking for: past it, unindexed views and queries start failing rather than slowing down. PnP needs its own app registration consent in most tenants now.',
    },
  ]),

  group('powershell', 'SQL Server', [
    {
      name: 'Invoke-Sqlcmd',
      task: 'run a query from a script',
      syntax: "Invoke-Sqlcmd -ServerInstance 'sql01\\prod' -Database master -Query 'SELECT name, state_desc FROM sys.databases' -TrustServerCertificate -QueryTimeout 120",
      module: 'SqlServer',
      effect: 'read',
      note: 'Since the driver change, a connection without -TrustServerCertificate or a real certificate fails outright where it used to work. The default query timeout is 600 seconds and the default connection timeout is much shorter, which is a confusing pair.',
      related: ['ps.get-dbadatabase'],
    },
    {
      name: 'Get-DbaDatabase',
      task: 'inventory databases across many instances',
      syntax: "Get-DbaDatabase -SqlInstance sql01, sql02 -ExcludeSystem | Select-Object SqlInstance, Name, RecoveryModel, LastFullBackup, SizeMB",
      module: 'dbatools',
      effect: 'read',
      note: 'A database in Full recovery with no log backup grows its log until the disk fills — that combination of columns is the check. dbatools is community-maintained but is the de facto standard; the Microsoft SqlServer module covers far less.',
    },
    {
      name: 'Backup-DbaDatabase',
      task: 'take a backup, or test that one restores',
      syntax: "Backup-DbaDatabase -SqlInstance sql01 -Database AppDb -Path \\\\backup\\sql -Type Full -CompressBackup",
      module: 'dbatools',
      effect: 'changes',
      note: 'A backup nobody has restored is not a backup. Test-DbaLastBackup actually restores it to a temporary database, runs DBCC against it, and drops it — that is the only check that proves anything.',
    },
    {
      name: 'Get-DbaLastGoodCheckDb',
      task: 'find databases with no recent consistency check',
      syntax: "Get-DbaLastGoodCheckDb -SqlInstance sql01 | Where-Object { $_.DaysSinceLastGoodCheckDb -gt 7 }",
      module: 'dbatools',
      effect: 'read',
      note: 'Corruption is found by DBCC CHECKDB, not by backups — and backups happily preserve corrupted pages for months. This is the report that tells you whether anyone has looked.',
    },
    {
      name: 'Get-DbaAgentJobHistory',
      task: 'find failed SQL Agent jobs',
      syntax: "Get-DbaAgentJobHistory -SqlInstance sql01 -StartDate (Get-Date).AddDays(-1) | Where-Object Status -ne 'Succeeded'",
      module: 'dbatools',
      effect: 'read',
      note: 'The job history table is capped by default at 1000 rows total and 100 per job, so a chatty job silently evicts everything else. Check that limit before trusting "there are no failures".',
    },
    {
      name: 'Get-DbaSpConfigure',
      task: 'compare instance settings against a baseline',
      syntax: "Get-DbaSpConfigure -SqlInstance sql01 | Where-Object { $_.ConfiguredValue -ne $_.DefaultValue }",
      module: 'dbatools',
      effect: 'read',
      note: 'The two that matter most are max server memory left at its default (SQL will take everything) and cost threshold for parallelism left at 5, which is a 1995 value and causes needless parallel plans.',
    },
    {
      name: 'Copy-DbaLogin',
      task: 'move logins between instances with their SIDs',
      syntax: "Copy-DbaLogin -Source sql01 -Destination sql02 -ExcludeSystemLogins",
      module: 'dbatools',
      effect: 'changes',
      note: 'Recreating a SQL login by hand gives it a new SID, which orphans every database user mapped to it — the database restores fine and nobody can log in. This copies the SID, which is the whole point.',
    },
    {
      name: 'Test-DbaConnection',
      task: 'check connectivity, ports and authentication to an instance',
      syntax: "Test-DbaConnection -SqlInstance sql01\\prod",
      module: 'dbatools',
      effect: 'read',
      note: 'It checks the SQL Browser, the resolved port, the authentication scheme and the edition in one call, which separates "the instance is down" from "a named instance and a blocked UDP 1434" far faster than a ping does.',
    },
  ]),

  group('powershell', 'Hyper-V and virtualisation', [
    {
      name: 'Get-VMHostSupportedVersion',
      task: 'see which VM configuration versions a host supports',
      syntax: 'Get-VMHostSupportedVersion',
      module: 'Hyper-V',
      effect: 'read',
      note: 'A VM moved to an older host will not start if its configuration version is newer, and the version is never downgraded. That one fact is behind most failed Hyper-V migrations.',
    },
    {
      name: 'Get-VMSnapshot',
      task: 'find Hyper-V checkpoints nobody removed',
      syntax: "Get-VM | Get-VMSnapshot | Where-Object CreationTime -lt (Get-Date).AddDays(-7)",
      module: 'Hyper-V',
      effect: 'read',
      note: 'Checkpoints are differencing disks: an old one grows without limit and merging it needs as much free space again. Removing one is not instant — the merge happens afterwards and the disk is busy until it finishes.',
    },
    {
      name: 'Set-VMProcessor',
      task: 'change vCPU count or enable nested virtualisation',
      syntax: 'Set-VMProcessor -VMName app01 -Count 4 -ExposeVirtualizationExtensions $true',
      module: 'Hyper-V',
      effect: 'changes',
      note: 'Nested virtualisation needs the VM off and dynamic memory disabled, and it is what WSL2 and Windows Sandbox need inside a VM. The error when dynamic memory is on does not mention memory.',
    },
    {
      name: 'Set-VMMemory',
      task: 'set dynamic memory limits',
      syntax: 'Set-VMMemory -VMName app01 -DynamicMemoryEnabled $true -MinimumBytes 2GB -StartupBytes 4GB -MaximumBytes 16GB',
      module: 'Hyper-V',
      effect: 'changes',
      note: 'Dynamic memory and SQL Server or Exchange are a bad combination: those applications take what is offered and never give it back, so the balloon driver cannot reclaim it and the host runs out.',
    },
    {
      name: 'Optimize-VHD',
      task: 'reclaim space from a dynamic virtual disk',
      syntax: "Optimize-VHD -Path D:\\VMs\\app01.vhdx -Mode Full",
      module: 'Hyper-V',
      effect: 'changes',
      note: 'The disk must be offline or attached read-only, and the guest should have been zero-filled first — otherwise it reclaims almost nothing and you conclude the tool does not work.',
    },
    {
      name: 'Move-VMStorage',
      task: 'move a VM\u2019s files to another volume while it runs',
      syntax: "Move-VMStorage -VMName app01 -DestinationStoragePath E:\\VMs\\app01",
      module: 'Hyper-V',
      effect: 'changes',
      note: 'A live storage migration is safe but slow and I/O heavy. It also silently leaves the old folder behind, which is how a volume you just emptied is still full.',
    },
    {
      name: 'Get-VMNetworkAdapter',
      task: 'see a VM\u2019s network adapters, VLANs and MAC addresses',
      syntax: 'Get-VM | Get-VMNetworkAdapter | Select-Object VMName, SwitchName, MacAddress, IPAddresses',
      module: 'Hyper-V',
      effect: 'read',
      note: 'A dynamic MAC changes when the VM moves hosts, which breaks DHCP reservations and any licence keyed to it. Set-VMNetworkAdapter -StaticMacAddress is the fix, and it needs the adapter down.',
    },
    {
      name: 'Enable-VMIntegrationService',
      task: 'turn on guest services such as file copy or time sync',
      syntax: "Enable-VMIntegrationService -VMName app01 -Name 'Guest Service Interface'",
      module: 'Hyper-V',
      effect: 'changes',
      note: 'Time synchronisation should be off on a domain controller — the host overrides the domain hierarchy and causes a slow, confusing authentication failure across the domain.',
    },
  ]),

  group('powershell', 'DNS, DHCP and addressing', [
    {
      name: 'Add-DnsServerResourceRecordA',
      task: 'create a DNS A record',
      syntax: "Add-DnsServerResourceRecordA -ZoneName example.com -Name app01 -IPv4Address 10.0.0.9 -TimeToLive 01:00:00 -ComputerName dc01",
      effect: 'changes',
      note: 'A record created by hand in an AD-integrated zone has no owner, so the machine cannot later update it and dynamic registration fails silently. Add -CreatePtr to get the reverse record in the same call.',
      related: ['ps.remove-dnsserverresourcerecord'],
    },
    {
      name: 'Remove-DnsServerResourceRecord',
      task: 'delete a DNS record',
      syntax: "Remove-DnsServerResourceRecord -ZoneName example.com -RRType A -Name app01 -Force -ComputerName dc01",
      effect: 'destructive',
      note: 'Without -RecordData it removes every record of that type with that name, which for a round-robin name is all of them. Export the zone with Export-DnsServerZone first; there is no undo.',
    },
    {
      name: 'Get-DnsServerZoneAging',
      task: 'check scavenging settings on a zone',
      syntax: 'Get-DnsServerZone -ComputerName dc01 | Get-DnsServerZoneAging',
      effect: 'read',
      note: 'Scavenging turned on with a no-refresh plus refresh interval longer than the DHCP lease deletes records for machines that are still live. Turned off, the zone fills with stale records nobody dares remove.',
    },
    {
      name: 'Get-DnsServerDiagnostics',
      task: 'turn on DNS query logging to find what is asking for something',
      syntax: 'Set-DnsServerDiagnostics -ComputerName dc01 -QueryLogging $true -Queries $true',
      effect: 'changes',
      note: 'It writes a great deal and will fill a disk on a busy server. Turn it on, capture the window you need, and turn it off — a scheduled task to disable it an hour later is a sensible precaution.',
    },
    {
      name: 'Get-DhcpServerv4Lease',
      task: 'see who currently holds an address',
      syntax: 'Get-DhcpServerv4Lease -ComputerName dhcp01 -ScopeId 10.0.0.0 | Sort-Object LeaseExpiryTime',
      effect: 'read',
      note: 'Leases in a Bad_Address state mean something answered a ping for an address the server was about to offer — a static address inside the pool, or a rogue server. That state is the clue, and it is easy to miss.',
    },
    {
      name: 'Add-DhcpServerv4Reservation',
      task: 'pin an address to a MAC',
      syntax: 'Add-DhcpServerv4Reservation -ComputerName dhcp01 -ScopeId 10.0.0.0 -IPAddress 10.0.0.50 -ClientId AA-BB-CC-DD-EE-FF -Name printer01',
      effect: 'changes',
      note: 'The reservation takes effect at the next lease renewal, not immediately; the client keeps its old address for up to half the lease. The ClientId format matters — dashes, not colons.',
    },
    {
      name: 'Get-DhcpServerv4ScopeStatistics',
      task: 'find scopes about to run out of addresses',
      syntax: 'Get-DhcpServerv4ScopeStatistics -ComputerName dhcp01 | Where-Object PercentageInUse -gt 85',
      effect: 'read',
      note: 'Run this before a big deployment rather than during one. A scope at 100% hands out nothing and the symptom on the client is an APIPA address, which looks like a switch problem.',
    },
    {
      name: 'Export-DhcpServer',
      task: 'back up or move DHCP configuration',
      syntax: 'Export-DhcpServer -ComputerName dhcp01 -File C:\\backup\\dhcp.xml -Leases -Force',
      effect: 'read',
      note: 'With -Leases it captures the current lease database too, which is what makes a migration invisible to clients. Import-DhcpServer needs the scopes not to exist yet on the target.',
    },
    {
      name: 'Get-NetIPConfiguration',
      task: 'see addresses, gateway and DNS together',
      syntax: 'Get-NetIPConfiguration -Detailed | Where-Object { $_.NetAdapter.Status -eq "Up" }',
      effect: 'read',
      note: 'The one cmdlet that shows address, gateway and DNS servers for the same adapter — the others each show a piece, which is how a wrong DNS server on the second NIC goes unnoticed.',
    },
    {
      name: 'Set-DnsClientServerAddress',
      task: 'set DNS servers on an interface',
      syntax: "Set-DnsClientServerAddress -InterfaceAlias 'Ethernet' -ServerAddresses 10.0.0.10, 10.0.0.11",
      effect: 'changes',
      note: 'Passing -ResetServerAddresses returns it to DHCP. Setting a domain member to a public resolver breaks domain logon, group policy and Kerberos in ways that look like everything else failing at once.',
    },
  ]),

  group('powershell', 'Clustering, storage and file services', [
    {
      name: 'Get-ClusterNode',
      task: 'see cluster nodes and their state',
      syntax: 'Get-ClusterNode -Cluster sqlclu01 | Select-Object Name, State, NodeWeight',
      module: 'FailoverClusters',
      effect: 'read',
      note: 'NodeWeight zero means that node does not vote in quorum — often set during maintenance and then forgotten, which quietly changes how many failures the cluster survives.',
      related: ['ps.get-clusterquorum'],
    },
    {
      name: 'Get-ClusterQuorum',
      task: 'check what keeps the cluster alive in a failure',
      syntax: 'Get-ClusterQuorum -Cluster sqlclu01',
      module: 'FailoverClusters',
      effect: 'read',
      note: 'A two-node cluster with no witness loses quorum when either node goes down — which is to say it survives nothing. That is the single most common cluster design mistake.',
    },
    {
      name: 'Move-ClusterGroup',
      task: 'fail a role over to another node',
      syntax: 'Move-ClusterGroup -Cluster sqlclu01 -Name "SQL Server (PROD)" -Node node02',
      module: 'FailoverClusters',
      effect: 'changes',
      note: 'It is a real failover with a real outage of a few seconds to a few minutes. Test-Cluster first, and check the preferred owners — a role can fail straight back and flap.',
    },
    {
      name: 'Test-Cluster',
      task: 'validate a cluster before and after changes',
      syntax: 'Test-Cluster -Node node01, node02 -Include "Storage Spaces Direct", Inventory, Network, "System Configuration"',
      module: 'FailoverClusters',
      effect: 'read',
      note: 'The full storage tests take the disks offline, so on a live cluster you must exclude Storage or use -Include as above. Microsoft support asks for this report first, so run it before you call.',
    },
    {
      name: 'Get-ClusterSharedVolume',
      task: 'see CSV ownership and state',
      syntax: 'Get-ClusterSharedVolume | Select-Object Name, State, OwnerNode',
      module: 'FailoverClusters',
      effect: 'read',
      note: 'A CSV in redirected access mode routes all I/O over the network through the owning node, which presents as everything being slow with no errors anywhere. Backup software is the usual cause.',
    },
    {
      name: 'Get-StoragePool',
      task: 'see Storage Spaces pools and free capacity',
      syntax: 'Get-StoragePool | Where-Object IsPrimordial -eq $false | Select-Object FriendlyName, HealthStatus, Size, AllocatedSize',
      effect: 'read',
      note: 'A thin virtual disk can be larger than the pool; when the pool actually fills, the volume goes read-only without warning. Watch AllocatedSize against Size, not the volume\u2019s free space.',
    },
    {
      name: 'Repair-VirtualDisk',
      task: 'rebuild a degraded Storage Spaces virtual disk',
      syntax: "Get-VirtualDisk | Where-Object HealthStatus -ne 'Healthy' | Repair-VirtualDisk",
      effect: 'changes',
      note: 'It runs in the background and Get-StorageJob is the only way to see progress. A repair that will not start usually means there is not enough free space in the pool to rebuild into.',
    },
    {
      name: 'Set-SmbServerConfiguration',
      task: 'turn off SMB1 or require signing',
      syntax: 'Set-SmbServerConfiguration -EnableSMB1Protocol $false -RequireSecuritySignature $true -Confirm:$false',
      effect: 'changes',
      note: 'Requiring signing costs measurable throughput on file servers and breaks some old appliances and scanners. Check Get-SmbSession for who is connected before changing it on a live server.',
    },
    {
      name: 'Get-SmbClientNetworkInterface',
      task: 'see whether SMB Multichannel is actually being used',
      syntax: 'Get-SmbMultichannelConnection | Select-Object ServerName, ClientInterfaceIndex, ClientRssCapable, ClientRdmaCapable',
      effect: 'read',
      note: 'Multichannel needs matching interface speeds and either RSS or RDMA at both ends; mismatched NIC speeds silently fall back to one channel, and a file copy runs at a quarter of the expected rate.',
    },
    {
      name: 'New-SmbShare',
      task: 'create a share with the right permissions',
      syntax: "New-SmbShare -Name Data -Path D:\\Data -FullAccess 'DOMAIN\\FileAdmins' -ChangeAccess 'DOMAIN\\Staff' -FolderEnumerationMode AccessBased",
      effect: 'changes',
      note: 'Share permissions and NTFS permissions are both applied and the more restrictive wins — the usual pattern is Everyone/Full at the share and real permissions on NTFS. AccessBased enumeration hides what a user cannot open.',
    },
    {
      name: 'Get-FsrmQuota',
      task: 'see or set file server quotas',
      syntax: 'Get-FsrmQuota | Select-Object Path, Size, Usage, SoftLimit',
      module: 'FileServerResourceManager',
      effect: 'read',
      note: 'A hard quota makes writes fail with a disk-full error the application reports as something else entirely. Soft quotas notify and let the write through, which is usually the right first step.',
    },
  ]),

  group('powershell', 'IIS and web servers', [
    {
      name: 'Get-IISSite',
      task: 'list sites, bindings and state',
      syntax: 'Get-IISSite | Select-Object Name, State, @{ n = "Bindings"; e = { $_.Bindings.bindingInformation -join ", " } }',
      module: 'IISAdministration',
      effect: 'read',
      note: 'IISAdministration is the current module; WebAdministration is the old one and its IIS: drive does not work reliably in PowerShell 7. Mixing the two in one script gives confusing state.',
      related: ['ps.get-iisapppool'],
    },
    {
      name: 'Get-IISAppPool',
      task: 'find stopped or recycling application pools',
      syntax: "Get-IISAppPool | Where-Object State -ne 'Started' | Select-Object Name, State, ManagedRuntimeVersion",
      module: 'IISAdministration',
      effect: 'read',
      note: 'A pool stopped by rapid-fail protection stays stopped until someone starts it — five crashes in five minutes and the site is down for good, with the reason only in the System event log.',
    },
    {
      name: 'Restart-WebAppPool',
      task: 'recycle an application pool',
      syntax: 'Restart-WebAppPool -Name AppPool01',
      module: 'WebAdministration',
      effect: 'changes',
      note: 'A recycle drops in-process session state, so any site not using out-of-process sessions logs every user out. That is the difference between a recycle at 3am and one at 3pm.',
    },
    {
      name: 'New-WebBinding',
      task: 'add an HTTPS binding with SNI',
      syntax: "New-WebBinding -Name 'Default Web Site' -Protocol https -Port 443 -HostHeader app.example.com -SslFlags 1",
      module: 'WebAdministration',
      effect: 'changes',
      note: 'SslFlags 1 is SNI, which is what lets several certificates share port 443. Creating the binding does not attach a certificate — that is a separate step against the IIS:\\SslBindings drive or netsh.',
    },
    {
      name: 'Get-WebConfigurationProperty',
      task: 'read a setting out of web.config or applicationHost.config',
      syntax: "Get-WebConfigurationProperty -Filter /system.webServer/security/requestFiltering -Name requestLimits.maxAllowedContentLength -PSPath 'IIS:\\Sites\\Default Web Site'",
      module: 'WebAdministration',
      effect: 'read',
      note: 'The default upload limit is about 28MB and a larger POST fails with a 404.13 that reads like a missing page. This is where that number lives, and it is set in two different places.',
    },
    {
      name: 'Get-ChildItem IIS:\\SslBindings',
      task: 'see which certificate is bound to which port',
      syntax: 'Get-ChildItem IIS:\\SslBindings',
      module: 'WebAdministration',
      effect: 'read',
      note: 'A certificate renewed in the store is not automatically rebound — the binding holds a thumbprint, so the site keeps serving the expired one until the binding is updated.',
    },
    {
      name: 'Get-WebRequestTracing',
      task: 'capture why a request is failing',
      syntax: "Enable-WebRequestTracing -Name 'Default Web Site'",
      module: 'WebAdministration',
      effect: 'changes',
      note: 'Failed request tracing produces one XML file per matching request and will fill a disk on a busy site. Scope it to the status code you are chasing, and turn it off afterwards.',
    },
  ]),

  group('powershell', 'Patching, Defender and the endpoint', [
    {
      name: 'Get-WindowsUpdateLog',
      task: 'turn the Windows Update ETW traces into a readable log',
      syntax: 'Get-WindowsUpdateLog -LogPath C:\\Temp\\WindowsUpdate.log',
      effect: 'read',
      note: 'Since Windows 10 there is no WindowsUpdate.log file — this cmdlet builds one from ETW, and it needs internet access for symbols unless you set -SymbolServer. That is why it fails on an isolated server.',
    },
    {
      name: 'Get-WUList',
      task: 'see what updates are pending',
      syntax: 'Get-WUList -MicrosoftUpdate',
      module: 'PSWindowsUpdate',
      effect: 'read',
      note: 'PSWindowsUpdate cannot install updates over a remote session in the usual way — the Windows Update agent refuses a network logon token. Invoke-WUJob schedules it as a task under SYSTEM, which is the workaround.',
    },
    {
      name: 'Install-WindowsUpdate',
      task: 'install updates and control the reboot',
      syntax: 'Install-WindowsUpdate -MicrosoftUpdate -AcceptAll -IgnoreReboot -Verbose',
      module: 'PSWindowsUpdate',
      effect: 'changes',
      note: '-IgnoreReboot installs and leaves the machine pending a restart, which is what a maintenance window wants: patch everything first, reboot in a controlled order afterwards.',
    },
    {
      name: 'Get-MpComputerStatus',
      task: 'check Defender is on and its definitions are current',
      syntax: 'Get-MpComputerStatus | Select-Object AMServiceEnabled, RealTimeProtectionEnabled, AntivirusSignatureLastUpdated, IsTamperProtected',
      effect: 'read',
      note: 'Definitions more than a few days old usually mean the machine cannot reach the update source, not that Defender is off. Tamper protection being on is why Set-MpPreference changes silently do not stick.',
      related: ['ps.get-mpthreatdetection'],
    },
    {
      name: 'Get-MpThreatDetection',
      task: 'see what Defender has found on a machine',
      syntax: 'Get-MpThreatDetection | Sort-Object InitialDetectionTime -Descending | Select-Object -First 20 ThreatID, Resources, InitialDetectionTime',
      effect: 'read',
      note: 'Resources is the path, and it is the field people actually need. Get-MpThreat gives the names and severities; the two have to be joined by ThreatID to make a report anyone can act on.',
    },
    {
      name: 'Add-MpPreference',
      task: 'add a Defender exclusion',
      syntax: 'Add-MpPreference -ExclusionPath D:\\SQLData -ExclusionProcess sqlservr.exe',
      effect: 'changes',
      note: 'Use Add-MpPreference, not Set-MpPreference: Set replaces the whole exclusion list and quietly removes everything already there. Exclusions are also an attacker\u2019s favourite hiding place, so keep them narrow.',
    },
    {
      name: 'Start-MpScan',
      task: 'run a scan on demand',
      syntax: 'Start-MpScan -ScanType QuickScan',
      effect: 'read',
      note: 'A full scan on a file server takes hours and hits the disks hard. Quick scan covers the places that matter for most detections; use -ScanPath for a targeted look at one directory.',
    },
    {
      name: 'Get-BitLockerVolume',
      task: 'check BitLocker status and recovery key ids',
      syntax: 'Get-BitLockerVolume | Select-Object MountPoint, VolumeStatus, ProtectionStatus, EncryptionPercentage',
      effect: 'read',
      note: 'ProtectionStatus Off with VolumeStatus FullyEncrypted means protection is suspended — usually left that way after a firmware update, which leaves the disk effectively unencrypted at boot.',
    },
    {
      name: 'Suspend-BitLocker',
      task: 'suspend BitLocker for a firmware or TPM change',
      syntax: 'Suspend-BitLocker -MountPoint C: -RebootCount 1',
      effect: 'changes',
      note: '-RebootCount 1 resumes protection automatically after one restart, which is the difference between a safe maintenance window and a fleet of machines left unprotected. Zero means it stays suspended indefinitely.',
    },
    {
      name: 'Get-LocalGroup',
      task: 'audit local administrators across machines',
      syntax: "Invoke-Command -ComputerName $servers { Get-LocalGroupMember -Group 'Administrators' } | Select-Object PSComputerName, Name, PrincipalSource",
      effect: 'read',
      note: 'PrincipalSource tells a local account from a domain one, which is the distinction that matters in an audit. The cmdlet fails on a machine whose group contains an orphaned SID rather than skipping it.',
    },
    {
      name: 'Get-AppxPackage',
      task: 'find or remove a Store app from an image',
      syntax: "Get-AppxPackage -AllUsers | Where-Object Name -like '*Xbox*' | Select-Object Name, PackageFullName",
      effect: 'read',
      note: 'Removing a package for current users does not stop it being installed for new ones — that needs Remove-AppxProvisionedPackage against the image. Doing only the first is why they come back.',
    },
  ]),

  group('powershell', 'More Azure and Graph', [
    {
      name: 'Get-AzContext',
      task: 'check which subscription you are about to change',
      syntax: 'Get-AzContext | Select-Object Name, Account, Subscription, Tenant',
      module: 'Az.Accounts',
      effect: 'read',
      note: 'The context is per-session and persisted between sessions by default, so a script run tomorrow may target a different subscription than the one you tested against. Assert it at the top rather than assuming it.',
      related: ['ps.set-azcontext'],
    },
    {
      name: 'Get-AzAdvisorRecommendation',
      task: 'pull cost and reliability recommendations',
      syntax: "Get-AzAdvisorRecommendation -Category Cost | Select-Object ImpactedValue, ShortDescription, ExtendedProperties",
      module: 'Az.Advisor',
      effect: 'read',
      note: 'The savings figures in ExtendedProperties assume you act on every recommendation and keep the resource for a year. Useful as a list of things to look at, misleading as a budget.',
    },
    {
      name: 'Get-AzPolicyState',
      task: 'find resources that break policy',
      syntax: "Get-AzPolicyState -Filter \"ComplianceState eq 'NonCompliant'\" | Group-Object PolicyDefinitionName -NoElement | Sort-Object Count -Descending",
      module: 'Az.PolicyInsights',
      effect: 'read',
      note: 'Compliance data lags by up to half an hour and a newly assigned policy takes even longer to evaluate. A resource showing compliant right after a change has probably not been evaluated yet.',
    },
    {
      name: 'New-AzRoleAssignment',
      task: 'grant a role at a scope',
      syntax: "New-AzRoleAssignment -ObjectId $sp.Id -RoleDefinitionName 'Contributor' -Scope '/subscriptions/xxxx/resourceGroups/rg-app'",
      module: 'Az.Resources',
      effect: 'changes',
      note: 'Assignments inherit down and cannot be denied lower — a Contributor at subscription scope cannot be removed from one resource group. Assign at the narrowest scope that works, because widening later is easy and narrowing is not.',
    },
    {
      name: 'Get-AzLog',
      task: 'find out who changed a resource',
      syntax: "Get-AzLog -ResourceGroupName rg-app -StartTime (Get-Date).AddDays(-7) | Where-Object { $_.OperationName.Value -like '*write*' } | Select-Object Caller, OperationName, EventTimestamp",
      module: 'Az.Monitor',
      effect: 'read',
      note: 'The activity log only keeps 90 days and only records control-plane operations — someone deleting rows in a database or a blob is not here. That needs diagnostic settings sent somewhere durable.',
    },
    {
      name: 'Get-AzNetworkWatcherNextHop',
      task: 'work out why traffic from a VM is not arriving',
      syntax: 'Get-AzNetworkWatcherNextHop -NetworkWatcher $nw -TargetVirtualMachineId $vm.Id -SourceIPAddress 10.0.1.4 -DestinationIPAddress 10.0.2.4',
      module: 'Az.Network',
      effect: 'read',
      note: 'It evaluates the effective route table for that exact pair, which settles a UDR argument in one call. Its companion, Test-AzNetworkWatcherIPFlow, does the same for NSG rules and names the rule that blocked it.',
    },
    {
      name: 'Get-MgGroupMember',
      task: 'list members of an Entra group, with paging',
      syntax: "Get-MgGroupMember -GroupId $id -All | ForEach-Object { $_.AdditionalProperties.userPrincipalName }",
      module: 'Microsoft.Graph.Groups',
      effect: 'read',
      note: 'Graph returns 100 items per page and the cmdlet stops there without -All. The member objects are directory objects, so the properties you want are in AdditionalProperties rather than typed on the object.',
      related: ['ps.invoke-mggraphrequest'],
    },
    {
      name: 'Invoke-MgGraphRequest',
      task: 'call a Graph endpoint the modules do not cover',
      syntax: "Invoke-MgGraphRequest -Method GET -Uri 'https://graph.microsoft.com/beta/identityGovernance/accessReviews/definitions' -OutputType PSObject",
      module: 'Microsoft.Graph.Authentication',
      effect: 'read',
      note: 'The escape hatch for anything in beta or newer than the module. It uses the existing Connect-MgGraph token, so the scopes you consented to still apply — a 403 here usually means a missing scope, not a missing permission.',
    },
    {
      name: 'Get-MgContext',
      task: 'see which scopes the current Graph session actually has',
      syntax: 'Get-MgContext | Select-Object Account, Scopes, AppName, TenantId',
      module: 'Microsoft.Graph.Authentication',
      effect: 'read',
      note: 'Most Graph failures are a missing scope rather than a missing role, and this is where you see it. Connect-MgGraph with new scopes does not add to the old ones — it replaces the session.',
    },
    {
      name: 'Update-MgUser',
      task: 'change a user attribute in Entra',
      syntax: "Update-MgUser -UserId user@example.com -Department 'Platform Engineering' -JobTitle 'Architect'",
      module: 'Microsoft.Graph.Users',
      effect: 'changes',
      note: 'In a hybrid tenant, attributes mastered on-premises are overwritten at the next sync — the change appears to work and silently reverts within half an hour. Change those in Active Directory instead.',
    },
  ]),

  group('powershell', 'More vSphere (PowerCLI)', [
    {
      name: 'Get-VMHostService',
      task: 'check or start services on an ESXi host',
      syntax: "Get-VMHost esx01 | Get-VMHostService | Where-Object Key -eq 'TSM-SSH'",
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'SSH left running on a host raises a configuration alarm and is a finding in every audit. Policy "on" means it starts at boot, which is different from it running now.',
    },
    {
      name: 'Get-AdvancedSetting',
      task: 'read or set an advanced setting on a host or VM',
      syntax: "Get-VMHost esx01 | Get-AdvancedSetting -Name Syslog.global.logHost",
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'A host with no syslog target keeps its logs on a ramdisk and loses them at every reboot, which is exactly when you want them. It is a one-line check across the whole cluster.',
    },
    {
      name: 'Get-VMHostNetworkAdapter vmk',
      task: 'check VMkernel adapters, MTU and services',
      syntax: 'Get-VMHost | Get-VMHostNetworkAdapter -VMKernel | Select-Object VMHost, Name, IP, Mtu, VMotionEnabled',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'One host with MTU 1500 in a cluster of 9000 gives vMotion failures that look random — they only fail for large transfers. Comparing the column across the cluster finds it in seconds.',
    },
    {
      name: 'Get-DrsRule',
      task: 'see affinity and anti-affinity rules',
      syntax: 'Get-Cluster | Get-DrsRule | Select-Object Cluster, Name, Enabled, Type, @{ n = "VMs"; e = { (Get-VM -Id $_.VMIds).Name -join ", " } }',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'A disabled anti-affinity rule still exists and still looks correct in a review, while both cluster nodes sit on one host. The Enabled column is the whole point of running this.',
    },
    {
      name: 'Get-VMHostStorage',
      task: 'check datastore paths and multipathing',
      syntax: 'Get-VMHost | Get-ScsiLun -LunType disk | Select-Object VMHost, CanonicalName, MultipathPolicy, CapacityGB',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'A LUN with a different multipath policy on one host is a performance mystery that only shows up under load. Round Robin is the usual answer for active-active arrays; check the vendor guidance before changing it.',
    },
    {
      name: 'Set-VM -MemoryGB',
      task: 'resize a virtual machine\u2019s CPU or memory',
      syntax: 'Set-VM -VM app01 -MemoryGB 16 -NumCpu 4 -Confirm:$false',
      module: 'VMware.PowerCLI',
      effect: 'changes',
      note: 'Without hot-add enabled the VM must be off, and hot-add itself carries a NUMA penalty on large VMs. Growing CPU beyond the host\u2019s physical core count per socket makes performance worse, not better.',
    },
    {
      name: 'Get-HardDisk',
      task: 'find thin, thick and independent disks',
      syntax: 'Get-VM | Get-HardDisk | Where-Object { $_.Persistence -ne "Persistent" -or $_.StorageFormat -eq "Thin" } | Select-Object Parent, Name, CapacityGB, StorageFormat, Persistence',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'An independent disk is not captured by snapshots and is therefore not captured by most backups — the backup succeeds and the data is not in it. This is the query that finds them before a restore does.',
    },
    {
      name: 'Get-VMGuest',
      task: 'see guest IP, hostname and VMware Tools state',
      syntax: 'Get-VM | Get-VMGuest | Select-Object VM, OSFullName, IPAddress, State, ToolsVersionStatus',
      module: 'VMware.PowerCLI',
      effect: 'read',
      note: 'Every guest-level operation — clean shutdown, quiesced snapshot, IP reporting — depends on Tools. A VM with Tools not running will be powered off hard by a maintenance script that assumed otherwise.',
    },
    {
      name: 'Start-VMHostService maintenance',
      task: 'put a host into maintenance mode and wait for it',
      syntax: "Set-VMHost -VMHost esx01 -State Maintenance -Evacuate -RunAsync | Wait-Task",
      module: 'VMware.PowerCLI',
      effect: 'changes',
      note: '-RunAsync with Wait-Task is how you avoid a cmdlet that appears to hang for forty minutes. Without -Evacuate on a cluster without full DRS automation, it waits for you to move the VMs by hand.',
    },
  ]),
];
