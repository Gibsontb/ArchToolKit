{"oci_identity_data_plane_generate_scoped_access_token":{"a":[["public_key","s","r","A temporary public key, owned by the service. The service also owns the corresponding private key. This public key will by put inside the security token by the auth service after successful validation of the certificate."],["scope","s","r","Scope definition for the scoped access token"]],"b":[["timeouts",1,0,0,{"a":[["create","s","o","(Defaults to 20 minutes), when creating the Generate Scoped Access Token"],["delete","s","o","(Defaults to 20 minutes), when destroying the Generate Scoped Access Token"],["update","s","o","(Defaults to 20 minutes), when updating the Generate Scoped Access Token"]]}]]}}